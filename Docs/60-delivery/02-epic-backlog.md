# Epic Backlog

> Status: Authoritative. Owner: Product + Engineering.
> Every epic carries: the persona jobs it serves, the module that owns it, its phase, and **acceptance criteria that can be objectively verified**. An epic without testable acceptance criteria is not ready to start.

## How to read this

- **Jobs** — IDs from `00-product/02-personas-and-jobs.md`.
- **Phase** — from `60-delivery/01-roadmap.md`.
- **Type** — `ENTRY` (buyer rejects without it), `DIFF` (where Atlas wins), `PARITY`, `FOUNDATION`.

---

## Phase 0 — Structural foundation

### E0.1 — Module scaffolding and boundary enforcement
**Module:** all · **Type:** FOUNDATION · **Priority:** P0

Create the target structure, the module template, and the import-linter ratchet.

**Acceptance**
- `src/atlas/{platform,modules,entrypoints}` exists with the module template generator.
- Import-linter runs in CI at a baseline that records current violations and **fails on new ones**.
- A generated module passes all contracts out of the box.

### E0.2 — Tier 0 invariant test suite
**Module:** all · **Type:** FOUNDATION · **Priority:** P0

**Acceptance**
- All nine invariant tests exist, pass, and cannot be skipped or marked `xfail`.
- `test_cross_tenant_denial` is generated by reflection over the route table, so a new endpoint is covered automatically.
- `test_no_source_values_in_control_plane` seeds sentinels and scans every table, log line, event payload, and trace.

### E0.3 — Extract `platform/`
**Module:** platform · **Type:** FOUNDATION · **Priority:** P0

**Acceptance**
- `platform-purity` contract passes: no domain imports.
- Application behaviour unchanged; all tests green.

### E0.4 — Split `models.py` / `schemas.py` into module schemas
**Module:** all · **Type:** FOUNDATION · **Priority:** P0

**Acceptance**
- Every ORM model lives in its module's schema (`ALTER TABLE … SET SCHEMA`, no data movement).
- No cross-schema foreign keys except into `identity`.
- Orphan-detection reconciliation runs before and after with identical counts.
- `module-privacy` contract passes.

### E0.5 — CI hardening
**Module:** — · **Type:** FOUNDATION · **Priority:** P0

**Acceptance**
- OpenAPI diff gate fails on an unversioned breaking change.
- Event-catalog assertion fails on an uncatalogued event type.
- SBOM generated; images signed; admission policy verifies.
- Performance regression gates configured.

---

## Phase A — Reach category minimum

### EA.1 — Oracle and BigQuery connectors to certified
**Module:** 02 · **Jobs:** P1, P2 · **Type:** ENTRY · **Priority:** P0

**Acceptance**
- Both adapters pass discovery, bounded profiling, cost estimation, governed query, and masking against real fixtures.
- 100-point control-plane certification.
- Executable vendor/version fixtures for at least two versions each.
- Capability flags derived from certification, not declared (INV-9).
- Oracle EXPLAIN capability is enabled **only** after a least-privilege `PLAN_TABLE` path is certified; until then it fails closed.

### EA.2 — Snowflake and Databricks connectors
**Module:** 02 · **Jobs:** P1 · **Type:** ENTRY · **Priority:** P0

**Acceptance** — as EA.1, plus warehouse-specific cost-model handling and workload-group awareness.

### EA.3 — Hybrid retrieval: vector, graph, fusion
**Module:** 12 · **Jobs:** A1, A3, S1 · **Type:** ENTRY · **Priority:** P0

**Acceptance**
- pgvector projection built from catalog and semantics, rebuildable.
- Graph expansion from seed hits, bounded.
- Fusion ranking with **every factor inspectable** in the evidence.
- Policy filtering happens **before** ranking; a leak test proves counts and ordering reveal nothing about unauthorized objects.
- Large-catalog benchmark at 1M objects: search first paint < 1 s p95.

### EA.4 — Global search and command palette
**Module:** 21, 12 · **Jobs:** A3 · **Type:** ENTRY · **Priority:** P0

**Acceptance**
- Cross-source, cross-type search from anywhere.
- Command palette (Ctrl/Cmd+K) with keyboard navigation.
- Results are policy-filtered and virtualized.

### EA.5 — Glossary term lifecycle
**Module:** 08 · **Jobs:** S1, S2, B2 · **Type:** ENTRY · **Priority:** P0

**Acceptance**
- Draft → review → approved → published → deprecated, with versions.
- Term-to-asset linkage, manual and inferred, with confidence.
- Synonym and acronym resolution feeding retrieval.
- Maker-checker through the unified queue (module 17).

### EA.6 — Ownership and stewardship
**Module:** 08 · **Jobs:** S3, S5 · **Type:** ENTRY · **Priority:** P0

**Acceptance**
- Individual, group, and **rule-based** ownership assignment.
- Bulk assignment by domain, schema, tag, pattern, or explicit selection.
- Unowned-asset backlog with routing.
- Leaver reassignment: an owner's whole portfolio reassigned in one action.
- Coverage scoring per domain and LOB, trended.

### EA.7 — Conflict resolution
**Module:** 08 · **Jobs:** S2, R1 · **Type:** ENTRY · **Priority:** P1

**Acceptance**
- Conflicting definitions raise a conflict record rather than last-write-wins.
- Both positions shown with source, author, timestamp, evidence.
- Resolution records a rationale; **the losing position is retained**.
- Resolution informs future precedence between the same sources.

### EA.8 — OpenLineage ingestion
**Module:** 09 · **Jobs:** A2, S4, U1 · **Type:** ENTRY · **Priority:** P0

**Acceptance**
- Accepts OpenLineage `RunEvent`; maps jobs, inputs, outputs, and `columnLineage`.
- Unresolvable datasets recorded as unresolved and **surfaced, never silently dropped**.
- Producer identity required; event size bounded; value-bearing facets rejected.

### EA.9 — View and stored-procedure lineage
**Module:** 09 · **Jobs:** A2, S4 · **Type:** ENTRY · **Priority:** P0

**Acceptance**
- View and procedure definitions parsed to column-level edges where the dialect permits.
- Literals redacted; definitions never executed.
- Confidence reflects parse completeness.

### EA.10 — MCP server and context products
**Module:** 19 · **Jobs:** A1, B3, P5 · **Type:** ENTRY + DIFF (W2) · **Priority:** P0

**Acceptance**
- MCP server exposing context products as resources and eligible tools as MCP tools.
- Stable context-product identity with immutable versions, declared purpose, owner, bounded asset scope, semantic and glossary versions, quality requirements, policy summary, and eligible tools.
- Draft, review, publish, supersede, reject, and deprecate lifecycle through the unified governance queue with maker-checker separation.
- **Policy evaluated on every read**, not only at connection.
- Consumption recorded as lineage edges.
- Tool invocation by an external agent goes through the **same query gateway** as a native run.
- Workload identity required; per-consumer rate limits and budgets.
- Value-freedom at the boundary verified by a sentinel test.

### EA.10a — Data products, contracts, and marketplace
**Module:** 19, 17 · **Jobs:** B1, B2, S1, S4, P5 · **Type:** ENTRY · **Priority:** P0

**Acceptance**
- Data product registry covers candidate, development, published, deprecated, and retired lifecycle states with domain, owner, ports, certification, quality posture, lineage coverage, and linked context products.
- Data contract registry versions schema, quality, freshness, SLA, producer, consumer, and product-port bindings and supports ODCS-compatible import/export.
- Breaking contract changes require compatibility evidence and an independently approved exception before publication.
- Marketplace lists only published, policy-visible products and exposes ownership, trust, usage terms, lineage, and quality without source values.
- Access requests are attributable, time-bound, independently approved where policy requires it, revocable, and audited.

### EA.10b — Deterministic governed context compiler
**Module:** 19, 07, 10 · **Jobs:** A1, S1, P5 · **Type:** ENTRY + DIFF · **Priority:** P0

**Acceptance**
- Versioned context specifications define graph traversal, included fields, target schema mapping, and delivery policy.
- The same approved inputs and specification produce the same canonical output hash.
- Built-in targets cover Snowflake Semantic Views, Databricks Metric Views, OSI, and ODCS; custom JSON/YAML schemas are supported without executable user code.
- Output is available through REST, MCP, and YAML export and never includes unapproved drafts or source values.
- Imported platform definitions can be reconciled against the governed version and produce an inspectable drift report.

### EA.10c — Unified AI registry and operational trust
**Module:** 15, 17, 19, 20 · **Jobs:** P5, U2, U4 · **Type:** ENTRY + DIFF · **Priority:** P1

**Acceptance**
- AI use cases, models, agents, versions, deployments, datasets, policies, owners, assessments, and runtime signals share one tenant-scoped registry.
- CLI/API manifests provide code-first registration without accepting credentials or source values.
- Trust scores are deterministic composites with every factor, input version, and missing signal visible.
- Assessment templates cover EU AI Act, NIST AI RMF, AI UC-1, and organization-defined controls.
- Runtime degradation can create remediation work, suspend a route or tool through existing deterministic controls, and retain evidence.

### EA.11 — Quality notification and escalation
**Module:** 11 · **Jobs:** P2, S5 · **Type:** ENTRY · **Priority:** P0

**Acceptance**
- Routing by domain owner, severity, and source.
- ITSM webhook integration.
- Escalation on unacknowledged incidents.
- Deduplication prevents alert fatigue at bank incident volumes.

### EA.12 — Freshness watermark contracts
**Module:** 11 · **Jobs:** A2 · **Type:** ENTRY · **Priority:** P0

**Acceptance**
- Watermark column configured per table with classification and retention treatment, approved via maker-checker.
- Freshness activates only for configured tables; others remain `NOT_CONFIGURED`.
- **Scan age is never presented as freshness** (ADR-0016), asserted by test.

### EA.13 — Bulk catalog operations and virtualization
**Module:** 04, 21 · **Jobs:** S3, A3 · **Type:** ENTRY · **Priority:** P1

**Acceptance**
- Bulk tag, classify, own, certify — by filter or explicit selection.
- Async above a threshold with per-item partial-success reporting.
- Rationale required for governed operations.
- 1M-row lists virtualized without browser lockup.

---

### EA.14 — Unified lineage graph and transitive impact
**Module:** 09 · **Jobs:** A2, S4 · **Type:** ENTRY · **Priority:** P0 · **Status:** DELIVERED 2026-08-29

Merges declared foreign keys, approved/candidate relationship edges, dbt manifest
dependencies, and OpenLineage table edges into one graph, and replaces direct-reference
counting with bounded transitive upstream/downstream traversal. Opened by
`review-2026-08/research/05-collibra-lineage-and-platform.md` against Collibra's
Catalog Lineage / Impact Analysis views.

**Acceptance**
- `GET /v1/datasources/{id}/unified-lineage/graph` returns one node/edge set spanning all four
  lineage sources, tagged by `edge_source`, bounded by `node_limit`/`edge_limit` with
  `truncation_reasons`. — **met**
- Nodes without a matched catalog table (unmatched dbt resources, unresolved OpenLineage
  datasets) are surfaced as synthetic nodes instead of silently dropped. — **met**
- `GET /v1/datasources/{id}/unified-lineage/impact/{node_id}` computes upstream and downstream
  impact by bounded multi-hop traversal (`aida.unified_lineage.traverse`), not direct-edge
  counting, with per-node contributing edge sources and depth. — **met**
- Pure traversal logic (`aida/unified_lineage.py`) is unit tested without a database, mirroring
  `aida/knowledge_graph.py`; contract tests assert the two routes are published and the
  response schemas carry no source values. — **met**, `tests/test_unified_lineage.py`

**Not yet delivered** (tracked separately): authoritative column-level mapping (LN-10),
view/procedure/BI nodes (LN-11), and graph export (LN-12).

## Phase B — Regulated-enterprise trust

### EB.1 — ABAC with agent-vs-human context
**Module:** 17 · **Jobs:** all · **Type:** ENTRY · **Priority:** P0

**Acceptance**
- Attributes: classification, purpose, residency, identity attributes from the IdP, and **agent-vs-human request context**.
- Decision p95 ≤ 50 ms.
- Every decision logs its inputs and pins the policy version.
- Policy simulation: "who could see this?"

### EB.2 — Bank OIDC certification and workload identity
**Module:** 01 · **Jobs:** P4 · **Type:** ENTRY · **Priority:** P0

**Acceptance**
- Bank issuer, claims, and group contract certified.
- Workload identity for connector agents and MCP consumers.
- Token revocation and replay policy.
- Break-glass with elevated audit.

### EB.3 — Certified secret adapter and delegated source identity
**Module:** 01, 02 · **Jobs:** P4 · **Type:** ENTRY · **Priority:** P0

**Acceptance**
- Bank Vault/CyberArk/cloud adapter registered and certified.
- Delegated, read-only source identities.
- Rotation drill: **zero failed requests**.
- Outage drill: bounded cache serves in-flight work, then denies.

### EB.4 — Source-side connector agents
**Module:** 02 · **Jobs:** P1 · **Type:** DIFF (W9) · **Priority:** P1

**Acceptance**
- Agent runs in a restricted zone, **outbound-only** mTLS to Atlas.
- Pushes the canonical envelope; no Atlas-initiated inbound connection.
- Heartbeat, version reporting, and remote upgrade.
- Network team acceptance: no inbound firewall exception required.

### EB.5 — Indirect-injection screening
**Module:** 13 · **Jobs:** A5, U2 · **Type:** DIFF · **Priority:** P0

**Acceptance**
- Retrieved metadata screened **before** entering model context.
- Content-origin attribution in the evidence.
- Corpus of malicious column and model descriptions: zero bypasses.
- Multilingual and obfuscated variants covered.

### EB.6 — Model-risk evaluation corpus and kill-switch drill
**Module:** 15, 13 · **Jobs:** P5, U2 · **Type:** DIFF · **Priority:** P0

**Acceptance**
- Bank-domain accuracy corpus with published results.
- Human red-team exercise completed.
- Kill switch drilled: **< 60 s to full stop**, deterministic paths unaffected, evidence retained.
- Refusal-rate and drift monitoring live.

### EB.7 — WORM archive, SIEM routing, retention enforcement
**Module:** 20 · **Jobs:** U1, U4 · **Type:** ENTRY · **Priority:** P0

**Acceptance**
- Audit exported to immutable storage.
- Security events routed to the SOC.
- Retention policy enforced per data class, with legal hold.
- OpenTelemetry export live.

### EB.8 — Source-native row/column policy
**Module:** 16 · **Jobs:** A1, U2 · **Type:** ENTRY · **Priority:** P0

**Acceptance**
- Source entitlements synchronized where the engine supports it.
- Dynamic masking and tokenization integration.
- Masking test suite covering aliases, derived expressions, and nested subqueries.

### EB.9 — Tool certification workflow
**Module:** 14 · **Jobs:** R1, U3 · **Type:** DIFF · **Priority:** P0

**Acceptance**
- Certification corpus per tool with expiry.
- Recertification triggered by dependency change.
- Certification state visible at invocation.

---

## Phase C — User experience

### EC.1 — Persona navigation from identity
**Module:** 21, 01 · **Jobs:** all · **Type:** ENTRY · **Priority:** P0

**Acceptance** — persona derived from approved OIDC groups; browser selection removed in production; every persona has a landing shell and no user is stranded.

### EC.2 — Studio
**Module:** 18 · **Jobs:** S1, A4 · **Type:** PARITY · **Priority:** P1

**Acceptance**
- Change sets group related edits; conflict detection against the base version.
- Test harness runs against **synthetic** fixtures before submission.
- Diff view showing the effect on published state.
- Parameter-contract designer for tools.
- Impact preview at submission.
- Submits as **one** proposal to the review queue.

### EC.3 — Bulk governance operations
**Module:** 17 · **Jobs:** R3, S3 · **Type:** ENTRY · **Priority:** P0

**Acceptance** — bulk decisions with per-item rationale; 10,000-item selection without freeze; delegation, time-bounded and audited; bulk approval is **one decision, not a bypass**.

### EC.4 — Accessibility audit and remediation
**Module:** 21 · **Jobs:** all · **Type:** ENTRY · **Priority:** P1

**Acceptance** — WCAG AA; keyboard navigation and focus order; ARIA correctness including live regions; screen-reader validation on primary flows; reduced-motion respected.

### EC.5 — Graph level-of-detail rendering
**Module:** 10, 21 · **Jobs:** S4 · **Type:** ENTRY · **Priority:** P1

**Acceptance** — large neighbourhoods render smoothly; **the API safety boundary is unchanged** (ADR-0010); truncation remains explicit.

---

## Phase D — Proof

### ED.1 — Bank-scale benchmark corpus
**Priority:** P0 · **Acceptance:** synthetic estate of 1M objects and 1,000 sources with realistic messiness — history tables, SCD, partial FKs, ambiguous names; reproducible generation; versioned with the code.

### ED.2 — Load, soak, spike suites
**Priority:** P0 · **Acceptance:** every target in `10-architecture/10-performance-and-scale-model.md` measured; results published; CI regression gates active.

### ED.3 — Resilience drills
**Priority:** P0 · **Acceptance:** projection rebuild, PITR restore, Temporal failover, credential rotation, batch forced-restart, kill switch — each run, timed, and evidenced.

### ED.4 — Security certification
**Priority:** P0 · **Acceptance:** penetration test against the threat model with no critical findings; adversarial SQL corpus per dialect with zero bypasses; prompt-injection corpus with zero bypasses.

---

## Phase E — Advantage

### EE.1 — Quality → runtime coupling
**Module:** 11, 12, 13, 14 · **Type:** DIFF (W1) · **Priority:** P1

**Acceptance**
- An open high-severity incident **demotes** the affected table in retrieval ranking.
- Any answer using an affected table carries a **visible trust warning** naming the incident.
- A governed tool whose dependency has an open incident is flagged or blocked per policy.
- Quality incidents appear in the impact graph.
- Sustained incidents expire asset certification.

### EE.2 — AI decision lineage as first-class edges
**Module:** 09, 13 · **Type:** DIFF (W3) · **Priority:** P0

**Acceptance**
- Every agent run emits `AI_DECISION` edges: retrieval selections with reasons, **rejections with reasons**, tool selection, versions pinned, refusals with the control that fired.
- Traversable in the same graph as data lineage.
- Value-free.

### EE.3 — Negative knowledge surface
**Module:** 06, 07 · **Type:** DIFF (W4) · **Priority:** P1

**Acceptance** — rejected inferences queryable as "what we decided is not true"; re-proposal suppressed; suppression lifts with a "previously rejected" flag when evidence changes materially.

### EE.4 — Compliance packs from runtime evidence
**Module:** 20 · **Type:** DIFF (W5) · **Priority:** P1

**Acceptance** — model risk, BCBS 239, access review, AI usage, and change control packs generated for a named period; reproducible; WORM-archived.

### EE.5 — Trust-scored answers
**Module:** 13, 11, 09 · **Type:** DIFF (W7) · **Priority:** P2

**Acceptance** — composite score from quality, freshness, semantic confidence, lineage depth, and tool approval; **every contributing factor is explainable**; the score never replaces the evidence, it summarizes it.

### EE.6 — Multi-step tool plans
**Module:** 13, 14 · **Type:** PARITY · **Priority:** P1

**Acceptance** — plans compose approved tools with step, time, token, and cost budgets; every step is validated and evidenced; a partial failure leaves consistent state.

### EE.7 — Public connector certification standard
**Module:** 02 · **Type:** DIFF (W6) · **Priority:** P1

**Acceptance** — the certification harness is published and third-party runnable; a third-party adapter passes without core changes.

### EE.8 — Data product and data contract registries
**Module:** 19 · **Type:** DIFF · **Priority:** P2 · **Status:** FOUNDATION DELIVERED, 2026-08-29

Delivers CP-2 and CP-3 from `20-modules/19-context-products-and-mcp.md` §15.2: versioned data
products with owners/ports/lifecycle/certification, and a data contract registry (schema,
quality, freshness, SLA, producer/consumer, ODCS-compatible) distinct from the
now-deleted src/aida/data_contracts.py's ingestion-envelope validation (orphaned duplicate of
runtime_contracts.py, removed 2026-08-31 under AU-6).

**Acceptance**
- Candidate → active → retired product lifecycle with maker-checker on publish (`DataProduct`,
  `DataProductVersion`, one-published-per-product constraint). — **met**,
  `src/aida/product_marketplace_api.py`, `migrations/versions/b4e8f2a71c90_*.py`
- Role-gated marketplace discovery (`DISCOVER`/`CONSUME` role bindings, `*` wildcard) filtered
  at the SQL level, not post-filtered in application code. — **met**,
  `search_marketplace` in `product_marketplace_api.py`
- Access-request workflow (request → governance review → approve/reject → revoke, one pending
  request per requester/version enforced by a partial unique index). — **met**
- Contract compatibility checks (`compatibility_status` derived as
  `INITIAL`/`COMPATIBLE`/`BREAKING` from field/rule diffs) block normal publication of a
  breaking change and route an exception through independent maker-checker approval. — **met**
- Pure compatibility, lifecycle, access-expiry, validation, and OpenAPI coverage is in
  `tests/test_agentic_platform.py`. — **met**; live PostgreSQL integration proof remains

### EE.9 — Governed context compiler
**Module:** 19 · **Type:** DIFF · **Priority:** P2 · **Status:** FOUNDATION DELIVERED, 2026-08-29

Delivers CP-5: deterministic mapping from the graph context (semantic models, metrics, terms,
ownership, policy, quality) to Snowflake Semantic Views, Databricks Metric Views, OSI, ODCS,
and custom YAML, delivered via REST, MCP, and file export.

**Acceptance**
- Deterministic, version-pinned compilation (`compile_context_product`) with a stable
  `artifact_hash` (canonical JSON, sorted keys) for a given input state. — **met**,
  `src/aida/context_compiler.py`
- Targets: MCP, REST, OSI, ODCS, Snowflake Semantic View, Databricks Metric View. — **met**
- Custom YAML target. — **met**: `YAML` emits deterministic idiomatic YAML with an
  `application/yaml` content type; JSON and YAML deployments are both accepted for drift.
- Drift report against a previously deployed definition (by hash or by content, with per-path
  diff). — **met**, `POST /v1/context-product-versions/{id}/compile/drift`
- Quality-gated: compilation of an unpublished/non-quality-passing version is denied for
  non-lifecycle roles, reusing `evaluate_context_product_quality_from_db`. — **met**
- File export delivery mode. — **met**, via the validated attachment endpoint with artifact
  hash and content-disposition headers
- Determinism, target, drift, raw-evidence rejection, and OpenAPI coverage is in
  `tests/test_agentic_platform.py`. — **met**; external target conformance fixtures remain

### EE.10 — Lineage MCP tools
**Module:** 09, 19 · **Type:** DIFF (W2) · **Priority:** P1 · **Status:** DELIVERED, 2026-08-29
(hardened further same day — see accomplishment log)

Delivers CP-6, using EA.14's unified graph as the data source: expose upstream, downstream,
impact, fuzzy entity resolution, and transformation-detail as MCP tools alongside the existing
catalog resources and governed tools in `mcp_server.py`. Opened further by
`review-2026-08/research/06-collibra-marketplace-and-mcp.md` against
Collibra's MCP Server page (25+ tools, read + write, fuzzy resolution).

**Acceptance**
- `atlas__get_lineage_graph` and `atlas__get_lineage_impact` are exposed as native MCP tools
  (not `GovernedToolVersion`-backed), reusing `unified_lineage_api`'s payload builders so the
  graph an MCP client sees can never drift from the REST one. — **met**
- Eligible-tool exposure: only callers whose roles intersect
  `UNIFIED_LINEAGE_READER_ROLES` see these tools in `tools/list`; an ineligible `tools/call`
  gets the identical "not found or not published" response used for a genuinely-unknown tool
  (same anti-enumeration shape as governed SQL tools). — **met**
- Unit tested without a database (role gating, argument validation, org-scoping, payload
  shape), mirroring `test_mcp_server.py`'s existing convention. — **met**,
  `tests/test_mcp_server.py`
- Fuzzy resolution (`resolve_entity` tool) and transformation-detail-as-a-tool
  (`get_transformation_detail`) implemented alongside the graph/impact tools, same
  authorization and dispatch path. — **met**, `src/aida/mcp_server.py`; corpus-scale fuzzy
  match quality not separately benchmarked — tracked as `MCP-3`
- Dedicated unit tests for `resolve_entity` and `get_transformation_detail` request/response
  shape, and a leak test proving policy filtering happens before traversal for these two
  tools specifically. — **met**, 2026-09-01: verified the role-eligibility check in
  `mcp_server.py::_handle_native_lineage_tool_call` runs before `datasource_id` parsing,
  before any `session.get`/traversal call. Two new tests in `tests/test_mcp_server.py`
  (`test_leak_resolve_entity_denied_caller_cannot_distinguish_existing_from_missing_entity`,
  `test_leak_get_transformation_detail_denied_caller_cannot_distinguish_existing_from_missing_entity`)
  drive each tool twice with a denied caller — once against an existing entity, once against a
  nonexistent one — asserting byte-identical anti-enumeration responses, with a spy session and
  monkeypatched traversal collaborators that raise if touched, proving no DB/traversal work runs
  for the denied path
- Consumption recorded as lineage: every successful native lineage tool call now writes
  `record_audit(action="mcp.lineage.read", ...)` and `record_outbox(event_type=
  "lineage.consumed.v1", ...)`. — **met**, closes the gap this file previously tracked as open
  (superseding the earlier "not met, same pre-existing gap as `resources/read`" note)

### EE.11 — Unified AI registry and trust scoring
**Module:** 19 · **Type:** DIFF · **Priority:** P2 · **Status:** FOUNDATION DELIVERED, 2026-08-29

Delivers CP-7 and CP-8: register AI use cases, models, agents, datasets, and policies with
full lifecycle and dependency graph; compute an explainable trust score from documentation,
lifecycle, quality, policy, evaluation, and runtime posture.

**Acceptance**
- Persistence for AI assets, versioned lifecycle, and assessments
  (`ai_asset`/`ai_asset_version`/`ai_assessment`, risk tier, framework-scoped control results).
  — **met**, `migrations/versions/b4e8f2a71c90_*.py`, `src/aida/models.py`
- Request/response contracts including an explainable `AiTrustScoreRead` (score, grade,
  per-factor breakdown, blockers). — **met**, `src/aida/platform_schemas.py`
- Tenant-scoped create/version/list/submit/assessment/trust APIs and maker-checker approval are
  mounted in `ai_registry_api.py` and `semantic_api.py`. — **met**
- The deterministic trust service exposes seven factors with a true 100-point ceiling and hard
  blockers for prohibited risk, critical incidents, failed/missing assessments, and weak
  high-risk evaluations. — **met**, `ai_registry.py`, `tests/test_agentic_platform.py`
- Managed EU AI Act/NIST template catalogs, remediation and maker-checker retirement APIs,
  provider evidence synchronization, deduplicated score history, and dependency graph APIs.
  — **met**; provider-specific certification and interactive graph visualization remain

## Related documents

- Roadmap: `60-delivery/01-roadmap.md`
- Tracker: `60-delivery/03-tracker.md`
- Personas and jobs: `00-product/02-personas-and-jobs.md`
