# Capability register — current state

> **Reconciliation 2026-09-11:** this register owns dated capability evidence, not work status. [Tracker section P](03-tracker.md#p-current-execution-queue-reconciled-2026-09-11) owns execution.
>
> **Refresh 2026-09-12 (R11-B14), completed.** Every row that still carried a 2026-09-06 measurement — 34 of them — was re-measured against the tree on 2026-09-12, one at a time, and now carries that date. Six rows had already been re-measured earlier the same day (the query execution gateway, Kafka/Redpanda outbox publication, Neo4j graph projection, and the three capabilities in the "added this cycle" section).
>
> **Four rows deliberately keep an earlier date, and were not re-measured in this pass:** the WORM audit archive (2026-09-09), the steward agent (2026-09-10), and the lineage and quality agents (2026-09-11). Each of those dates is the date of the dated run its Verified cell describes, and each is recent; re-stamping them 2026-09-12 without re-running that evidence would be exactly the drift this register exists to stop.
>
> The date column is the claim, not the row's presence here: a Yes means someone checked it on the date shown, not that it holds forever. What moved in this pass, and what was found to have been *wrong* rather than merely stale, is recorded in the [2026-09-12 re-measurement note](#appendix-what-the-2026-09-12-re-measurement-changed) at the foot of this file.

## Source-correction follow-up ? confirmed September 12

| Capability / property | Implemented / reachable | Configured / verified | Current work |
|---|---|---|---|
| Contracted-agent Ask and governed MCP tools | Yes: `api.py` and `mcp_server.py` pass resolved caller contract into orchestrator | Production configuration not inspected; boundary tests exist in `test_ar06_contract_on_live_paths.py`, not rerun in this documentation pass | X7 removal cancelled; R11-C6 retains other contract-boundary gaps |
| Safe unattended reviewer approvals | Guards and truth-labelled benchmark exist; independent correctness remains insufficient | Latest recorded September 12 result: 9 false twins approved in 14 pairs, no pairs distinguished; this pass did not rerun that benchmark | R11-C3 PARTIAL; keep unattended review off |
| Single source-SQL execution boundary | Native-policy apply removed; preview retained; provider-backed signatures and boundary guards implemented | Native-policy/signing/Tier-0 focused tests passed 2026-09-12; no new source execution performed here | R11-D1/D2 DONE |
| Ontology publication in running deployment | Code/lifecycle tests exist; both ontology tables confirmed in configured PostgreSQL | Read-only check 2026-09-12: database and repository at `090b3be72b67`; live draft/review/publication not exercised by this pass | R11-C1 PARTIAL; obsolete migration blocker resolved |
| Legacy UI retirement | Removed; `ui/` absent in current tree | Source/filesystem confirmation 2026-09-11; no live deployment inspected | UX-16 closed; navigation consolidation is R11-S10 |



## September 12 — capabilities added this cycle

Same column rules as the main register: *Verified* stays strict, so a green test against
in-memory SQLite is **No**, not Yes.

| Capability / property | Implemented | Reachable | Configured | Verified | Measured | Owner | Evidence |
|---|---|---|---|---|---|---|---|
| Impact analysis reaches BI reports | Yes — BI joins the unified graph at table grain with provenance, report containment walked upward | Yes — `unified_lineage_builder` collectors run in the existing traversal | Yes — no setting required | **No** — bounds and tenant isolation are proven by mutation-tested unit tests, not against a live BI import in a running deployment | 2026-09-12 | Engineering | `src/aida/unified_lineage_builder.py`; `tests/test_unified_lineage.py` (R11-B13 tests) |
| Classification propagation produces derived values | Yes — collector builds column edges from reviewed view/procedure/OpenLineage lineage and stores derived rows | Yes — a scheduler pass calls it | **No by design** — `classification_propagation_interval_minutes` defaults to 0 (never), following the task-agent convention; the pass opens no session when off | **No** — proven against in-memory SQLite only; no estate has run it, so no derived row exists anywhere yet | 2026-09-12 | Engineering | `src/aida/classification_propagation.py`; `src/aida/workflows/scheduler.py`; `tests/test_at11_classification_propagation.py` |
| Ask collects a governed tool's parameters | Yes ? structured required-parameter refusal and version-pinned retry; propose-as-tool action tested | Yes ? live endpoint and Ask screen | Yes on the seeded sample estate ? parameterized tool seeded through maker-checker | **Partial ? local deployment evidence recorded** in the acceptance guide/B1: structured 409 followed by `GOVERNED_TOOL`; 44 focused UI tests rerun this pass, including Ask/tool proposal/routes. No corporate deployment certification | 2026-09-12 | Engineering | `src/aida/api.py`; `ui-next/src/screens/AskScreen.tsx`; `ui-next/src/components/SaveAnalysisTool.tsx`; R11-B1; [acceptance guide](../40-engineering/14-acceptance-testing-guide.md) |
| Vector similarity contributes to retrieval ranking | Yes — the vector channel scores the authorized candidate set through `src/aida/retrieval_stages.py`, preferring the persisted index and embedding live what the index does not cover | Yes — inside `hybrid_retrieve_enhanced`, on the live Ask path | Yes as of 2026-09-12 — `gemini-embedding-001` at 768 dimensions; `compose.yaml` now passes the `AIDA_EMBEDDING_*` variables through, which it previously did not, so a container read `unset` however the host was configured | **Partial** — a real provider was called and returned 768-dimension vectors with the expected semantic ordering, and the persisted index was built (10 objects); not exercised against a bank-scale estate, and the channel **re-ranks rather than discovers**, so semantic retrieval of an object lexical never surfaced does not work — measured, 5 of 5 such corpus cases miss | 2026-09-12 | Engineering | `src/aida/retrieval_stages.py`; `src/aida/vector_index_service.py`; `Docs/10-architecture/19-embeddings-design.md`; `tests/test_retrieval_stages.py` |
| Persisted vector index is kept fresh without an operator | Yes — `run_vector_index_rebuild_pass` sweeps the stalest organizations on a cadence | Yes — called from `run_scheduler_iteration`; before 2026-09-12 the only caller was an operator endpoint whose UI does not exist, so the index went stale and retrieval silently paid a provider call per candidate per query | Yes — enabled by default and a no-op with no embedding provider, which is the shipped state | **No** — proven by unit tests including an AST check that the scheduler still calls it; no deployment has run the pass on a schedule | 2026-09-12 | Engineering | `src/aida/vector_index_service.py`; `src/aida/workflows/scheduler.py`; `tests/test_vector_index_schedule.py` |

## September 15: database footprint

Same column rules. Status for these capabilities lives in R11-D16 and R11-FP07; the rest
of the program is R11-FP01–FP18.

| Capability / property | Implemented | Reachable | Configured | Verified | Measured | Owner | Evidence |
|---|---|---|---|---|---|---|---|
| Stored SQL is value-free in every dialect, routine bodies included | Yes. `PARSED` requires a value-aware lexical scan of the rendered text to find nothing left; dollar-quoted, `Command`, `Block` and `RawString` text is scrubbed lexically and labelled `LEXICAL`. **Before 2026-09-15 dollar-quoted PostgreSQL/Snowflake routine bodies were stored with their literals under `PARSED`** (R11-D16) | Yes. One choke point: `ingestion._store_source_sql` for every view definition and routine body, the gateway's audit redaction, and dbt compiled SQL | n/a | **Partial.** Unit-proven over nine body shapes in three dialects, with precise T-SQL and Oracle output unchanged. On this repository's platform database the repair dry run found 0 stored routine bodies and 0 view definitions (3 dbt resources, none leaking). A deployment that ingested PostgreSQL or Snowflake routines before the fix keeps their literals until rescanned or repaired with `scripts/reredact_stored_sql.py` | 2026-09-15 | Engineering | `src/aida/sql_redaction.py`; `tests/test_sql_redaction_program_bodies.py`; `tests/test_inv6_value_freedom.py`; `scripts/reredact_stored_sql.py` |
| PL/pgSQL routine lineage | Yes. Temp tables (including `ON COMMIT`) as intermediates, `EXECUTE` as dynamic SQL, `PERFORM`, `RETURN QUERY`, `SELECT INTO` and `:=` as routine-local state, `EXCEPTION` handlers, `FOR ... IN <query> LOOP`, and any dollar-quote tag; a nested call to a captured routine is read through, within depth and callee bounds, its edges marked `via_routine` (`aida.routine_call_descent`); a table-valued function source is the function, never its schema, and is read through the same way; `DO $$ ... $$` and a T-SQL inline function body parse; a one-source statement attributes its unqualified columns | Yes. The lineage agent and a person's parse read the stored body through `require_eligible_routine_body`, which now accepts both value-free tiers | n/a (the lineage agent is still off by default: interval 0) | **Partial.** Proven on stored-form fixtures, including equivalence between redacted and raw lineage. The sample pack's native SQL ran on PostgreSQL 17.10. The lineage agent runs against the live sample estate and proposes what it read (`tests/test_nested_callee_lineage_live.py`, both engines) | 2026-09-15 | Engineering | `src/aida/procedure_lineage.py`; `src/aida/routine_call_descent.py`; `tests/test_procedure_lineage_plpgsql.py`; `tests/test_routine_call_descent.py`; `tests/test_nested_callee_lineage_live.py`; `tests/test_database_footprint_samples.py`; `Docs/90-reference/procedure-lineage-capability-matrix.md` |
| Discovery takes in only a selected scope, and never retires what it leaves out | Yes. Kinds, schema and `schema.object` globs are applied to connector output before persistence; a FULL run counts existing out-of-scope objects as seen | Yes. GET/PUT/preview routes and the Discovery scope panel in Source administration; `discover_datasource` applies the selection | **No by design.** No datasource has a selection until one is saved; unrestricted behaves exactly as before | **Partial.** In-memory SQLite runs of the real discovery activity, with a control run proving the deletion it prevents; migration verified up/down/up on scratch PostgreSQL. Schema scope is pushed into the PostgreSQL and SQL Server metadata queries and verified against both live sample sources; object kinds and object patterns, and the other connectors, still filter after reading | 2026-09-15 | Engineering | `src/aida/discovery_selection.py`; `src/aida/connectors/schema_scope.py`; `src/aida/workflows/activities.py`; `tests/test_discovery_selection.py`; `tests/test_schema_scope.py`; `tests/test_discovery_pushdown_live.py`; `ui-next/src/screens/SourcesScreenDiscoveryScope.tsx` |
| Every recorded footprint gap is counted per source, with the route that closes it | Yes. `aida.footprint_gaps` counts withheld, truncated and quarantined code, lineage backlog, unparsed statements, calls that could not be read through (routed to the source administrator), undecided edges, source-change holds and pending change signals (with queue age), each routed AGENT, HUMAN_REVIEW, SOURCE_ACCESS, OPERATIONS or EXPLAINED | Yes. `GET /v1/organizations/{id}/footprint-gaps` and the Operations screen's Knowledge gaps panel | n/a | **Partial.** In-memory SQLite including a gate-denied datasource that contributes nothing; no cost or quota metrics yet | 2026-09-15 | Engineering | `src/aida/footprint_gaps.py`; `src/aida/footprint_gaps_api.py`; `tests/test_footprint_gaps.py`; `ui-next/src/screens/OperationsFootprintGaps.tsx` |
| A routine's definition history is kept, and a package is a package | Yes. `metadata_routine_definition_version` appends a version per first capture and per moved definition (LITERAL_ONLY or STRUCTURAL); PACKAGE is a selectable, pushable kind whose grants follow it and that tool generation refuses (`PACKAGE_NOT_CALLABLE`); native `*_FUNCTION` kinds select as FUNCTION | Yes. Every pull and push ingestion path; discovery selection and its UI; the procedure-tool blueprint and tool agent | n/a | **Partial.** In-memory SQLite and pure tests; both migrations verified on scratch PostgreSQL. Package members (Oracle `ALL_PROCEDURES`, with overloads) and native subtypes (SQL Server, BigQuery) are stored; no history read route yet, and no live Oracle run | 2026-09-15 | Engineering | `src/aida/envelope_models.py`; `src/aida/ingestion.py`; `src/aida/discovery_selection.py`; `tests/test_routine_identity.py` |
| Retrieval gains from footprint enrichment are measured and gated | Yes. `quality_benchmark.run_footprint_benchmark` runs one corpus before and after `enrich_footprint` (routines with reviewed and undecided lineage, a published ontology concept) and scores recall and gap preservation | Yes. The AG-8 CLI gate (`scripts/quality_benchmark.py`) and its test file | n/a | **Partial.** Deterministic in-memory catalog: recall 0.0 before, 1.0 after (4 cases), gap preservation 1.0; answer correctness over enriched context not run (live route), thresholds await the domain owner | 2026-09-15 | Engineering | `scripts/quality_benchmark.py`; `tests/fixtures/quality_benchmark_corpus/footprint_enrichment_corpus.json`; `Docs/90-reference/quality-benchmark-baseline.json` |
| A refused description proposal does not come back unchanged | Yes. `asset_description_service.refusal_reason` refuses a proposal whose text, edited-from machine text or evidence signals match a REJECTED draft, and one whose words were approved and withdrawn; columns include `origin` in their evidence | Yes. The steward agent's table and column capabilities, and both generate routes | n/a | **Partial.** In-memory SQLite and pure tests, including the steward agent end to end; a glossary link is never proposed again once decided, since its proposal key names the link; a relationship is suppressed by N4 negative knowledge | 2026-09-15 | Engineering | `src/aida/asset_description_service.py`; `src/aida/column_description_service.py`; `tests/test_description_refusal.py` |
| A view's description draft describes it as a view, from what Atlas holds of its definition | Yes. Evidence records object kind, definition state (CAPTURED, TRUNCATED, QUARANTINED, WITHHELD, NOT_CAPTURED) and a digest of the stored value-free definition; the draft never quotes the definition; a captured definition scores as structural evidence; a view's draft is not published once its definition has moved since drafting (`DEFINITION_MOVED`) | Yes. Every table-description draft path: the generate route and the steward agent; approval through the governance queue, bulk decisions and the reviewer agent | n/a | **Partial.** In-memory SQLite and pure tests; routine descriptions await an R11-S4 decision | 2026-09-15 | Engineering | `src/aida/asset_description_service.py`; `tests/test_asset_description_views.py` |
| A source change holds only what it can have broken | Yes. `aida.change_signal_processing`: a redefined view or retired table opens a CRITICAL `SOURCE_CHANGE` incident (dependent governed tools fail closed through DQ-3), a reshaped table a WARNING, and a structurally redefined view or routine becomes eligible again for the lineage agent; each signal ends PROCESSED with its action. A governed tool extracted from a routine is bound to the definition its SQL was copied from: approval refuses it once that routine moved, and execution blocks it (`aida.tool_source_binding`) | Yes. `run_change_signal_processing_pass` in `run_scheduler_iteration`; the lineage agent's candidate queries; the routine hold on both tool execution paths | **No by design.** `change_signal_processing_interval_minutes` defaults to 0 because the pass opens holds; the lineage re-examination and the routine hold need no setting | **Partial.** In-memory SQLite through real rescans, the tool gate, the REST execution path and the lineage agent; both source-routine migrations and their backfills verified on scratch PostgreSQL. No estate has run the pass | 2026-09-15 | Engineering | `src/aida/change_signal_processing.py`; `src/aida/workflows/scheduler.py`; `src/aida/lineage_agent.py`; `src/aida/tool_source_binding.py`; `tests/test_change_signal_processing.py`; `tests/test_tool_source_binding.py` |
| A rescan records which source objects changed, and how | Yes. `metadata_change_signal` rows from the persistence write points: DEFINITION_CHANGED (LITERAL_ONLY or STRUCTURAL), STRUCTURE_CHANGED (COLUMNS_ADDED, COLUMNS_RETURNED, COLUMNS_REMOVED or COLUMNS_RETYPED), DEPRECATED (SIGNATURE_CHANGED, naming the replacing routine, when one new signature replaced it), REACTIVATED, PERMISSION_CHANGED (GRANT_ADDED, GRANT_MODIFIED or GRANT_REVOKED), and MEANING_PUBLISHED on ontology approval; idempotent by fingerprint comparison | Yes. Every pull and push ingestion path; the discovery receipt's `changes`; `GET /v1/datasources/{id}/change-signals` | n/a (always on; a first scan records nothing) | **Partial.** In-memory SQLite through both persistence halves; the three migrations verified up/down/up and compared with the ORM on scratch PostgreSQL. The FP16 processing pass consumes structural and retirement signals; permission classes are recorded and by design place no hold, since a source grant authorizes nothing in Atlas; a newly published ontology re-pins context products through the rebuild | 2026-09-15 | Engineering | `src/aida/change_signals.py`; `src/aida/ingestion.py`; `src/aida/workflows/activities.py`; `tests/test_change_signals.py`; `tests/test_change_signal_classes.py` |
| A discovery run says what kind of thing it took in, and how completely | Yes. `analysis_run.discovery_receipt`: per kind discovered and excluded, per facet SUPPORTED or UNSUPPORTED from the connector's own flags, view definitions and routine bodies counted captured, withheld and truncated, stream state, and what reconciliation did. Value-free | Yes. Written by `discover_datasource` with every committed batch, marked COMPLETE at finalize and INTERRUPTED on failure or cancel; served on every `AnalysisRunRead`; summarized per run in Source administration | n/a (every run from this migration on has one; older runs have none) | **Partial.** In-memory SQLite runs of the real activity, including a stream that fails after its first batch; migration `8e1d4b6a2c90` verified up/down/up on scratch PostgreSQL. Not run live; the push-ingestion path writes no receipt; PERMISSION_DENIED is not yet a facet outcome | 2026-09-15 | Engineering | `src/aida/discovery_receipt.py`; `src/aida/workflows/activities.py`; `tests/test_discovery_receipt.py`; `ui-next/src/screens/SourcesScreenAdmin.tsx` |
| A stored procedure or function is reachable by a question | Yes. ROUTINE candidate in the lexical stage, with graph edges to its reviewed tables; the SQL model gets the tables, never the routine; MCP `resolve_entity` covers ROUTINE; a routine is in the persisted vector index under its `schema.name`, so the vector stage no longer embeds it live per question | Yes. Inside `hybrid_retrieve` and `hybrid_retrieve_enhanced` on the live Ask path, and in MCP | n/a | **Partial.** In-memory SQLite, including graph expansion end to end and the index entry pinned by the hash of the text the live path would have composed; not measured against the R11-B2 answer corpus | 2026-09-15 | Engineering | `src/aida/retrieval.py`; `src/aida/retrieval_stages.py`; `tests/test_retrieval_routines.py` |
| A view is reachable by what its definition is built from | Yes. The lexical stage scores the stored value-free definition of an ACTIVE, AVAILABLE view whose screening allows it, at 0.6 of its overlap, never displacing a name match; the hit carries a digest, never the text | Yes. Inside `hybrid_retrieve` and `hybrid_retrieve_enhanced`, on the live Ask path and in MCP | n/a (always on where a definition is captured) | **Partial.** In-memory SQLite through the real retrieval: found by its definition, ranked under a name match, quarantined and withheld definitions never scored, and another datasource's view never read. No live estate measurement of the recall it adds | 2026-09-16 | Engineering | `src/aida/retrieval.py`; `tests/test_retrieval_view_definitions.py` |
| A context product binds the approved ontology meaning it was built on | Yes. `context_product_version.ontology_version_ids`, validated to APPROVED versions of the product's organization and pinned by version id; omitted from the fingerprint and compiled references while empty | Yes. Create draft's ontology-version picker; compile, download, drift and the MCP resource read carry the bound ids and the bound versions' meaning (`load_ontology_meaning`) | n/a (nothing is bound until an author picks a version) | **Partial.** In-memory SQLite and pure compiler tests; migration verified up/down/up on scratch PostgreSQL. Retrieval is scoped by datasource, not product, and uses the published version by design; a product's consumers read the bound meaning from its compiled and MCP payloads, screened on the way out | 2026-09-15 | Engineering | `src/aida/context_product_api.py`; `src/aida/context_compiler.py`; `src/aida/context_product_coverage.py`; `tests/test_context_product_ontology_binding.py`; `tests/test_context_product_ontology_meaning.py`; `ui-next/src/screens/ContextProductDraft.tsx` |
| A context product names routines and reports what its routines and views stand on | Yes. `context_product_version.routine_ids`, validated to ACTIVE routines in the product's project; `aida.context_product_coverage` resolves routine and view coverage (release gate, lineage state, completeness of parsing, reads/writes cut to the product's own tables, digest of stored value-free text) | Yes. Compile, download and drift routes, the MCP context-product resource read, and the routine picker on Create draft backed by `GET /v1/projects/{id}/context-product-routine-options` | n/a (a product names none until an author picks one; views are reported from existing table ids) | **Partial.** In-memory SQLite and pure compiler tests, including byte-identical legacy artifacts and fingerprints and no mention of tables outside the product; migration verified up/down/up on scratch PostgreSQL. No published product with routines exists anywhere yet | 2026-09-15 | Engineering | `src/aida/context_product_coverage.py`; `src/aida/context_compiler.py`; `tests/test_context_product_routines.py`; `ui-next/src/screens/ContextProductDraft.tsx` |
| The footprint backlog is visible without anyone looking at a screen | Yes. `aida.footprint_metrics` publishes gap counts by kind, the fleet's longest unprocessed change-signal wait, and how many organizations the sweep read -- last-observation gauges, with the gap kind as the only label | Yes. A scheduler pass every five minutes, on by default, exported on the existing `/metrics` endpoint | Yes (`footprint_metrics_enabled`, `footprint_metrics_interval_seconds`) | **Partial.** Unit-proven: zeros published so a drained queue is a reading, fleet totals summed, the longest wait taken rather than an average, no tenant identifier in any label, a failed organization leaving a visibly partial sweep, and an AST check that the scheduler calls the pass. No deployment scrapes them, so no alert threshold has been set or tested | 2026-09-16 | Engineering | `src/aida/footprint_metrics.py`; `src/aida/workflows/scheduler.py`; `tests/test_footprint_metrics.py` |
| A recorded gap names the objects behind its count | Yes. `GET /v1/datasources/{id}/footprint-gaps/{kind}` lists identity, qualified name and the record's own stable code, bounded at 200 with `truncated`; the gap whose objects were never read names none and says why | Yes. The Knowledge gaps panel expands a count into the list, fetched on demand | n/a | **Partial.** In-memory SQLite through the real route: each kind's objects, another source's objects absent, the unnameable kind's sentence, and an unknown kind or a source the caller may not read both refused as not found. Not exercised against a bank-scale estate, where the 200 bound is what a person sees | 2026-09-16 | Engineering | `src/aida/footprint_gap_detail.py`; `src/aida/footprint_gaps_api.py`; `tests/test_footprint_gap_detail.py`; `ui-next/src/screens/OperationsFootprintGaps.tsx` |
| A pushed snapshot records the same scope receipt a pulled one does | Yes. One `DiscoveryReceipt` per batch in `process_metadata_ingestion_batch`: each chunk observed once on the change-recording pass, reconciliation recorded for a FULL batch and refused with its reason for an INCREMENTAL one, and an INTERRUPTED receipt kept when a batch fails | Yes. Every chunked push through the ingestion API; served on `AnalysisRunRead` and summarized on the run card like a pulled run's | n/a | **Partial.** In-memory SQLite through the real activity: a two-chunk FULL batch counted once (the reapply pass does not double it) with its reconciliation recorded, and an INCREMENTAL batch naming why it reconciled nothing. No live push from a real producer; the path applies no discovery selection, so its scope is whatever was sent | 2026-09-16 | Engineering | `src/aida/batch_ingestion.py`; `tests/test_push_ingestion_receipt.py` |
| A run says how much of the source its own login may not see | Yes. `Connector.count_invisible_objects`, implemented for PostgreSQL over `pg_class` (the exact negation of `information_schema`'s visibility rule, tables and views only -- matviews and routines are read from the open catalog and are never hidden); carried per kind in the run receipt, where `null` is "could not be asked" rather than none | Yes. Every discovery run asks once, before its stream; the run card in Source administration says it in words; `SOURCE_OBJECTS_INVISIBLE` is a footprint gap routed to the source administrator | n/a (always asked; a connector that cannot answer reports UNKNOWN) | **Partial.** Against real PostgreSQL: a least-privilege role told how much it cannot see, the owner told nothing is hidden, an excluded schema not counted as hidden, and a routine in an ungranted schema still discovered. Unit tests for the receipt and the gap. The other five connectors inherit the honest `None`: each filters its metadata views by permission and keeps the unfiltered catalog behind a grant a least-privilege scanning principal is not meant to hold | 2026-09-16 | Engineering | `src/aida/connectors/postgres.py`; `src/aida/discovery_receipt.py`; `src/aida/footprint_gaps.py`; `tests/test_invisible_objects_live.py`; `ui-next/src/screens/SourcesScreenAdmin.tsx` |
| A rename is followed instead of retiring what reads the old name | Yes. Where the held table carries an approved rename's `superseded_by_table_id` and the successor is ACTIVE in the same source, the rebuild pass regenerates a view-generated tool from the successor's definition and writes any other tool's SQL against the new name (`aida.table_rename_rewrite`), as a DRAFT in the review queue; a data dictionary's rows follow the same rename when they are mapped again | Yes. Inside the `aida.context_rebuild` pass, alongside the retirement proposal it replaces, and in `remap_document` | n/a (a rename is followed only where a steward approved one) | **Partial.** In-memory SQLite through the real pass and the real review routes: rebuilt against the renamed table and the hold released once approved, a rejected rewrite followed by the retirement with neither proposed twice, a successor that is retired or in another source not followed, a document row following the rename once, and a restored name read as itself. Only a table rename is followed: nothing records a renamed column | 2026-09-16 | Engineering | `src/aida/table_rename_rewrite.py`; `src/aida/context_rebuild.py`; `src/aida/document_ingestion.py`; `tests/test_table_rename_rewrite.py`; `tests/test_context_rebuild.py`; `tests/test_document_ingestion.py` |
| A context product says when each source behind it was last read | Yes. `load_source_freshness` reports, per datasource behind the product's own tables, the last completed discovery run and the last completed FULL one, with the product's table ids grouped under the source they live in | Yes. `generated_from.source_freshness` on the compile, download and drift routes, and a `freshness` section in the MCP context-product read, both rendered by `context_compiler.freshness_section` | n/a (a source with no completed run reports no time -- the state in which Ask already refuses to answer) | **Partial.** In-memory SQLite: both times reported apart, an unfinished run is not a read, an incremental read never dates the last full one, a second source dated only by its own runs, another organization's tables resolving to nothing, and the compiled artifact byte-identical with and without it. Not measured on a live estate; freshness is per source, not per object, because a discovery run's reach is a datasource | 2026-09-16 | Engineering | `src/aida/context_product_coverage.py`; `src/aida/context_compiler.py`; `tests/test_context_product_freshness.py` |
| An ontology concept maps to views and routines, and its mappings report drift | Yes. `OntologyMapping.subject_type` accepts TABLE, VIEW, COLUMN and ROUTINE, checked against the catalog object's kind; `resolve_mapping_targets` reports VALID, TARGET_MISSING, TARGET_DEPRECATED or KIND_MISMATCH | Yes. The ontology version routes (writes refuse anything not VALID; the listing reports `mapping_validity`) and the ontology manager | n/a | **Partial.** In-memory SQLite through the real routes, including drift after deprecation. No ontology has been published live (R11-C1) | 2026-09-15 | Engineering | `src/aida/ontology_api.py`; `tests/test_ontology_typed_mappings.py`; `ui-next/src/components/OntologyManager.tsx` |
| A question in the business's words reaches the tables its approved ontology concept maps to | Yes. ONTOLOGY_CONCEPT candidate in the lexical stage, from each ontology's published APPROVED version only, standing on mappings valid in the asking datasource; graph edges to the mapped tables; the SQL model gets the tables; the version id rides in hit metadata | Yes. Inside `hybrid_retrieve` and `hybrid_retrieve_enhanced` on the live Ask path | n/a (nothing happens until an organization publishes an ontology) | **Partial.** In-memory SQLite including graph expansion and model context; not measured against the R11-B2 corpus, and no ontology is published anywhere yet | 2026-09-15 | Engineering | `src/aida/retrieval.py`; `src/aida/retrieval_stages.py`; `src/aida/ontology_kinds.py`; `tests/test_retrieval_ontology_concepts.py` |
| Views and read-only routines are proposed as governed tools automatically | Yes. The `agent:tool` task agent drafts through `tool_drafts` (the same validation a person's draft gets) and submits each draft to the T2 tool review; declines carry stable codes; reruns do not duplicate, and neither does a source a live tool already stands on -- matched by source binding whatever the slug, and for a view by the names its SQL reads | Yes. `GET`/`POST /v1/organizations/{org}/tool-agent[/run]` and the Tools tab; the scheduler once its interval is above 0 | **No.** Nothing ships registered (an approved AGENT-kind version and contract are required), and the interval defaults to 0 | **No.** In-memory SQLite only; no estate has run it | 2026-09-15 | Engineering | `src/aida/tool_agent.py`; `src/aida/tool_drafts.py`; `tests/test_tool_agent.py` |
| A join is approved only on evidence that one table references the other | Yes. `aida.relationship_validation` works out evidence classes, key columns, uniqueness, cardinality, direction, optionality and profile observation bounds from the catalog and value-free profile counts. Approval is refused with `RELATIONSHIP_NAME_MATCH_ONLY` unless a declared foreign key, query-history joins, or a key on exactly one side for a non-generic column name supports it; the validation an approval rested on is stored with the candidate, and read only under each side's metadata gate and, across data domains, a cross-boundary grant | Yes. The single, bulk and composite decision routes; `GET /v1/relationship-candidates/{id}/validation` and `GET /v1/composite-relationship-candidates/{id}/validation`; the Relationships detail panel | n/a (always on) | **Partial.** In-memory SQLite through the real decision and read routes, including a key dropped after approval reading as CORROBORATION_LOST, and composite-key discovery persisting on a real session. No live estate; the source-side inclusion check is recorded NOT_RUN; drift acts: after a scan, a join whose evidence is gone is suspended back to review and restored when its evidence returns exactly as approved | 2026-09-15 | Engineering | `src/aida/relationship_validation.py`; `src/aida/relationship_validation_api.py`; `src/aida/relationship_drift.py`; `tests/test_relationship_validation.py`; `tests/test_relationship_drift.py`; `tests/test_relationship_validation_authorization.py`; `ui-next/src/screens/RelationshipsScreen.tsx` |
| A query can call only built-in or operator-authorized functions | Yes. `SqlGuard` refuses a call the parser does not recognise as a dialect built-in, and every schema- or package-qualified call, with `UNAUTHORIZED_FUNCTION`, unless named in `sql_guard_allowed_functions`; the adversarial denylist still wins | Yes. Every guard: the query gateway (Ask, tools and MCP), tool drafts, SQL validation and the evaluations | Supplied (`sql_guard_allowed_functions`; empty means nothing user-defined is callable) | **Partial.** Unit tests and adversarial-corpus cases in every dialect, with the gateway, orchestrator and tool suites; the built-in allowlist covers documented built-ins probed against the parser, so an undocumented or newer built-in is refused until named | 2026-09-15 | Engineering | `src/aida/sql_guard.py`; `tests/test_sql_guard.py`; `tests/fixtures/adversarial_sql_corpus/postgres.json` |
| A source change is rebuilt through review, and answered correctly again | Yes. `aida.context_rebuild` supersedes lineage a redefined view no longer produces, drafts regenerated tool versions, descriptions and re-pinned context product versions into their review queues, and resolves a view's or table's hold only once nothing standing on it is stale, proposing DEPRECATE reviews for the tools reading a retired table and re-scoping its context products; maps proposed data dictionaries again after a scan, proposing claims for new matches and refusing approval of claims on retired objects; re-pins context products to a newly published ontology version, and from superseded semantic model and glossary term versions to the current ones; view and routine tools are bound to the definition they were generated from (`aida.tool_source_binding`) | Yes. `run_context_rebuild_pass` in `run_scheduler_iteration`; the view-to-tool route, the procedure-tool route and the tool agent record the binding | **No by design.** `context_rebuild_interval_minutes` defaults to 0 | **Partial.** An end-to-end journey against real PostgreSQL and SQL Server sources (discover, investigate, review, publish, ask and answer 150, redefine, hold, rebuild, approve, ask and answer 160) on a SQLite platform, and SQLite unit tests, including retired, returned, reshaped and retyped tables; migrations verified on scratch PostgreSQL. No deployment runs the pass; Ask's model-generation path is not exercised; the SQL Server run needs the local sample container | 2026-09-15 | Engineering | `src/aida/context_rebuild.py`; `src/aida/tool_source_binding.py`; `tests/test_footprint_journey.py`; `tests/test_context_rebuild.py` |

> Status: **Current state, not history.** Owner: Engineering lead.
> Created 2026-09-06 for finding D06 of [`../review-2026-09-05/REVIEW.md`](../review-2026-09-05/REVIEW.md).
> Every row carries a date, an owner and evidence a reader can go and check.

## September 16: the 2026-09-16 review's findings

Same column rules, and they bite hard here: **`Verified` stays strict**, so a green test
against in-memory SQLite is **No**, not Yes. Status lives in section P -- R11-FP04, FP07,
FP08, FP12, FP13, FP17 and the new R11-D17. Dispositions, and what was deliberately not
done, are in [the reconciliation](25-review-reconciliation-2026-09-16.md).

| Capability / property | Implemented | Reachable | Configured | Verified | Measured | Owner | Evidence |
|---|---|---|---|---|---|---|---|
| A context product bounds what an answer may execute, on every execution path | Yes -- `QueryExecutionGateway.validate`/`execute` take an optional scope and refuse inside `_run_validation`, in the same phase as the datasource allowlist and before the phase that opens a connector, so an out-of-scope statement is refused with no execution session in existence. The boundary only ever intersects; a caller passing nothing gets the pre-F01 pipeline | Yes -- the Ask route, MCP `tools/call`, the direct-SQL route and governed-tool execution all reach it; INV-2's import-linter contract still reports KEPT | Yes -- no setting required | **Partial** -- the boundary itself is proven against in-memory SQLite only, but it is now *deployed*: the rebuilt API declares `context_product_key` on `AgentAnalysisRequest`, which the image the review inspected did not. No estate has yet refused a real out-of-scope statement | 2026-09-16 | Engineering | `src/aida/context_product_execution_scope.py`; `src/aida/query_gateway.py`; `tests/test_f01_context_product_execution_boundary.py` |
| A nested CTE cannot delete a physical table from the parsed reference list | Yes -- `sql_guard`'s CTE-alias set is scoped to each table's own position in the statement. It had been one flat set over the whole statement, so a CTE declared inside a subquery shadowed an identically-named physical table elsewhere and removed it from `referenced_tables` -- escaping the catalog allowlist, the ABAC axes and the product boundary at once | Yes -- every gateway path parses through it | Yes -- no setting required | **Partial** -- the shadowing case is proven by unit test and the pre-change failure was reproduced against `81c75d5`; no adversarial corpus sweep for other shadowing shapes | 2026-09-16 | Engineering | `src/aida/sql_guard.py`; `tests/test_sql_guard.py` |
| A routine has an Atlas-authored, versioned, reviewed description | Yes -- `RoutineDocumentation`, `RoutineDocumentationVersion` (which names the definition version its prose describes) and `RoutineDescriptionDraft`, reviewed through the existing queue with one adapter entry and a T0 tier. The body is never quoted | Yes -- routes mounted, review queue filterable, withdrawal accepts `ROUTINE` | Yes -- no setting required | **No** -- in-memory SQLite only; no estate has drafted or approved a routine description | 2026-09-16 | Semantics lead | `src/aida/routine_description_service.py`; `src/aida/routine_description_api.py`; `tests/test_routine_description.py`; `tests/test_routine_description_body_states.py` |
| Profile statistics reach the people they were measured for, with their limitation first | Yes -- `ProfilePanel` renders `GET /v1/tables/{id}/profile` in the evidence pane: the scope sentence before any number, an uncomputed facet as its reason rather than a blank, and a withheld column kept in the list with its marker | Yes -- mounted in `EvidencePane`, and withheld when the pane's own read was refused so one denial does not answer with two alerts | Yes -- no setting required | **No** -- jsdom tests only; not seen against a real profile in a browser | 2026-09-16 | Profiling lead | `ui-next/src/components/ProfilePanel.tsx`; `ui-next/src/components/ProfilePanel.test.tsx` |
| A profile says whether it saw the whole table, correctly, on every engine | Yes -- observation scope is a stored facet. Two engines had been wrong: BigQuery set `row_count_estimate` from `sampled_row_count` so a bounded profile claimed FULL, and Snowflake issued no bound yet reported SAMPLE | Yes -- written by the profiling activity, read by `ProfileBounds` and by relationship approval | Yes -- no setting required | **Partial** -- the per-engine tests build each connector's own SQL and assert the bound, but only PostgreSQL and SQL Server have live fixtures; BigQuery, Snowflake, Oracle and Databricks are proven by unit test only | 2026-09-16 | Profiling lead | `src/aida/connectors/bigquery.py`; `src/aida/connectors/snowflake.py`; `tests/test_connectors_profile_facets.py` |
| Each engine's capability is published per facet, and SUPPORTED is not confused with certified | Yes -- one generated matrix at six facets per engine and native object kind, derived from code at generation time, with a `--check` staleness mode and a live route. Facet state, registry `maturity` and `live_validation` are separate columns on purpose | Yes -- `GET /v1/engines/capability-matrix`, and the generated reference doc | Yes -- no setting required | **Partial** -- `test_capability_matrix_matches_certification` (listed Planned since 2026-08-30, now written) proves the matrix agrees with what the code can do. It cannot prove an engine works: `live_validation` reads EXERCISED for PostgreSQL and SQL Server only, and that is the honest state | 2026-09-16 | Connector lead | `src/aida/engine_capability_matrix.py`; `Docs/90-reference/engine-capability-matrix.md`; `tests/test_engine_capability_matrix.py` |
| A facet whose read the source refuses is recorded, not swallowed | Yes -- `aida.capability_states` adds `PERMISSION_DENIED`, which existed nowhere in the code before, so such a facet failed its whole run instead of being reported | Yes -- carried on the discovery receipt and the capability matrix | Yes -- no setting required | **No** -- no source has refused a facet under this code path; the classification is proven by unit test | 2026-09-16 | Connector lead | `src/aida/capability_states.py`; `tests/test_capability_states.py` |
| Whether a routine was fully understood is recorded, not re-derived | Yes -- a per-object parse-coverage record persists completion, statement count and unparsed reasons. `is_fully_parsed` had been computed and returned but stored nowhere, so the question required hunting for UNPARSED edges | Yes -- route mounted; the gap register reads it | Yes -- no setting required | **No** -- in-memory SQLite only | 2026-09-16 | Metadata lead | `src/aida/procedure_lineage_models.py`; `tests/test_routine_parse_coverage.py` |
| dbt macros and hooks are visible as a limitation rather than counted as understood | Yes -- a model's macro-dependency count and a project's `on-run-*`/`pre_hook`/`post_hook` presence are reported as coverage evidence. `macros` is still deliberately not ingested, and nothing claims an expansion was resolved into lineage | Yes -- reported through the dbt ingestion path | Yes -- no setting required | **No** -- fixture manifests only; no customer dbt project measured | 2026-09-16 | Metadata lead | `src/aida/dbt_artifacts.py`; `tests/test_dbt_macro_hook_coverage.py` |
| Answer correctness over enriched context can be evaluated | Yes -- corpus and harness carrying expected answers, expected evidence, expected refusals as first-class verdicts, and provider-reported token usage kept distinguishable from an estimate | Yes -- a CLI, deliberately **not** in the blocking CI gate | **No** -- acceptance thresholds are unset and are the domain owner's decision; the harness says no threshold set rather than inventing a pass mark | **No** -- and this is the point of the row: **the evaluation has not been run.** It needs paid model calls and an enriched live datasource, and the sample source seeds three tables with no routines, lineage or ontology | 2026-09-16 | AI quality lead | `scripts/answer_evaluation_benchmark.py`; `tests/test_answer_evaluation_gate.py` |
| The metrics the platform emits can be scraped and alerted on | Yes -- scrape configuration and rules over what is already measured: footprint gap gauges, oldest pending change signal, projection lag and backlog, retrieval duration, HTTP error rate | Yes -- `infra/monitoring/`, with a profile-gated Compose service | **Partial** -- thresholds needing an operator's number ship as documented placeholders, and `aida_footprint_gaps` carries no tenant label by design, so these rules are not per-tenant | **No** -- **no deployment scrapes them, so nothing has fired.** A rule file is not an alert | 2026-09-16 | Platform lead | `infra/monitoring/`; `scripts/generate_prometheus_rule.py` |
| Parser and model spend is attributed per source, and quotas are enforced per tenant and source | Yes -- `cost_metrics` keeps provider-reported usage distinguishable from an estimate and never adds a connector-shaped `plan_cost` to a token count; `usage_quotas` reuses the agent-budget atomic-window pattern and restates `organization_id` per INV-5 | Yes -- reached from the model gateway and the admission path | **Partial** -- quota settings are registered with their decisions and ship permissive | **No** -- SQLite only; no estate has hit a quota, and no change-burst latency measurement has been taken (the harness exists and was not executed) | 2026-09-16 | Platform lead | `src/aida/cost_metrics.py`; `src/aida/usage_quotas.py`; `tests/test_usage_quotas.py`; `scripts/scale_harness/fp17_change_burst_latency.py` |
| Deployment parity against source can be detected | Yes -- `scripts/check_deployment_parity.py` compares applied revision against head by name, the live OpenAPI path and schema sets against the committed baseline, every required readiness dependency, and whether the deployed image's `Settings` carries every setting the source declares | Yes -- a CLI with a `--check` mode | Yes -- no setting required | **Yes** -- run 2026-09-16 against the rebuilt stack: **8 matched, 0 drifted, 0 not measured**, and it is the check that caught the stale image behind a healthy API (270 declared settings, where the deployed image had been missing one). An unreachable deployment still reports cannot tell, which is not parity. **2026-09-19: it also reported parity against an image missing two fixes**, because a code-only change moves no schema, route or setting; a fifth comparison now matches a digest of the shipped source (`build.source_digest`) against the checkout, and an image without one is not measured rather than matched | 2026-09-19 | Platform lead | `scripts/check_deployment_parity.py`; `src/aida/source_identity.py`; `tests/test_deployment_parity.py`; `tests/test_source_identity.py`; [runbook](../40-engineering/16-deployment-alignment-and-enablement-runbook.md) |

## September 17: the follow-on build cycle

Same column rules, and `Verified` stays strict -- a green test against in-memory SQLite is
**No**, not Yes. Several rows below do earn a Yes, because the live PostgreSQL and SQL Server
sample sources were used and then the result was deployed and parity-checked. Status lives in
section P -- R11-FP01, FP02, FP06, FP08, FP12, FP17, R11-D17 and the new R11-D18.

| Capability / property | Implemented | Reachable | Configured | Verified | Measured | Owner | Evidence |
|---|---|---|---|---|---|---|---|
| A trigger and a sequence are discovered, persisted and reconciled as themselves | Yes -- their own models, kinds and per-engine queries, not folded into routines and tables. A trigger carries its firing table, event, timing and either a body or the function it names; a sequence carries its declaration and owning column. A sequence's current position is never read, and PostgreSQL reads `pg_sequence` rather than `pg_sequences` so `last_value` is unreachable rather than merely unselected (INV-6) | Yes -- both pull paths and the push path, selectable in discovery scope, counted on the receipt, and published in the engine capability matrix | Yes -- no setting required | **Yes** -- against live PostgreSQL and SQL Server: create, scan, assert fact by fact, drop, rescan, and the same row id reads DEPRECATED rather than vanishing. Then deployed and parity-verified, with `metadata_trigger`/`metadata_sequence` present in the configured database and `TRIGGER`/`SEQUENCE` answering on the live capability matrix | 2026-09-17 | Connector lead | `src/aida/envelope_models.py`; `src/aida/ingestion.py`; `tests/test_trigger_sequence_persistence.py`; `tests/test_triggers_sequences_persist_live.py` |
| One refused read costs one facet, not the whole scan | Yes -- classified from the driver's own SQLSTATE, recorded on the receipt, and the run completes. Adopted across all six adapters, including the four synchronous drivers whose reads are captured in a thread and replayed into the classifier, because passing one connection between pool threads is what pytds and databricks-sql forbid | Yes -- every facet query on every adapter routes through it; a refused *inventory* is recorded and then still re-raised, because a FULL run over zero objects would retire the estate | Yes -- no setting required | **Yes** -- proven on live PostgreSQL with `EXECUTE` on `pg_get_viewdef` revoked from a least-privilege login: the run COMPLETED, `view_definitions` read PERMISSION_DENIED, the other six facets captured normally, and no driver message reached the receipt. Only PostgreSQL is live-proven; the other five are proven against their driver's own error class, and the row below records which of them can classify at all | 2026-09-17 | Connector lead | `src/aida/connectors/discovery.py`; `tests/test_facet_refusal_adapters.py`; `tests/test_facet_refusal_live.py` |
| A refusal is told apart from a failure without reading the driver's message | Yes -- SQLSTATE only, never the message, because a driver message can quote a value (INV-6). asyncpg and snowflake-connector report one and classify as refusals; databricks-sql reports `42501` in `exc.context["sqlState"]` rather than as an attribute, which the classifier now reads | Yes -- one classifier, shared by every adapter and by the invisible-object count | Yes -- no setting required | **Partial by design** -- pytds, oracledb and google-api-core report no SQLSTATE, so their refusals record UNAVAILABLE rather than PERMISSION_DENIED. That is the under-claiming direction INV-9 requires: a gap reported as "we did not get it" is honest, a failure reported as a denial would be a guess about the source's intent | 2026-09-17 | Connector lead | `src/aida/capability_states.py`; `tests/test_capability_states.py` |
| A pushed snapshot is governed by the same discovery selection a pull scan is | Yes -- the push path applied none before, so a pushed snapshot was in scope by definition. The contract is ignore-but-report: an out-of-scope delivered object is not persisted, is counted per kind on the receipt beside a real `selection_fingerprint`, and is reported back to the sender | Yes -- both chunked and unchunked push routes | Yes -- no setting required | **No** -- in-memory SQLite only; no estate has pushed a snapshot under a narrowed selection | 2026-09-17 | Connector lead | `src/aida/batch_ingestion.py`; `tests/test_push_selection.py` |
| A routine's definition history is readable, without releasing the body | Yes -- ordinal, capture time, capturing run, availability, truncation, change class and a digest, plus each version's read/write table footprint derived by re-parsing the stored value-free text and diffed against the version before it. The body is never served, and a withheld or quarantined version keeps its row with a marker rather than being omitted | Yes -- `GET /v1/routines/{routine_id}/definition-history`, rendered as a timeline inside the Operations knowledge-gap expansion | Yes -- no setting required | **No** -- in-memory SQLite and jsdom only. The route is deployed and answers, but no estate has read a real routine's history through it | 2026-09-17 | Metadata lead | `src/aida/definition_history_api.py`; `tests/test_definition_history.py`; `ui-next/src/components/DefinitionHistoryPanel.tsx` |
| An approved routine description is retrievable, and a routine appears in the worklist | Yes -- an APPROVED version joins the lexical stage with its own reason codes, carrying a digest and never the prose; only APPROVED ranks. The worklist ranks a routine on borrowed usage -- the query volume of the tables its ACTIVE lineage says it writes -- because nobody queries a procedure and a table-shaped score rates every routine zero | Yes -- inside `hybrid_retrieve`'s existing lexical stage (no new channel, per R11-S3), and on the existing worklist endpoint via `subject_type` | Yes -- no setting required | **No** -- SQLite only. The quality-benchmark gate is unmoved and its baseline deliberately not accepted, because the run reports `path=LIVE_EMBED` and the figure therefore depends on a provider being configured | 2026-09-17 | Retrieval lead | `src/aida/retrieval.py`; `src/aida/documentation_worklist.py`; `tests/test_retrieval_routine_descriptions.py`; `tests/test_documentation_worklist_routines.py` |
| Interactive latency under a change burst is measured rather than assumed | Yes -- a harness that drives the burst and reports percentiles for an interactive read under load and at rest | Yes -- `scripts/scale_harness/fp17_change_burst_latency.py`, and it cleans up what it creates | Yes -- no setting required | **Yes, and the answer is a split** -- 500,000 signals at 5,762/s for 86.8s: the catalog page holds (p95 16.1 to 31.1ms) while the footprint-gap read degrades (p95 46.8 to 148.3ms, p99 to 187.5ms), reproduced three times. **It proves the shape of the degradation and no magnitude at any target scale** -- one laptop, one reader, no HTTP -- so R11-B15 stays BLOCKED and nothing here touches it | 2026-09-17 | Platform lead | `scripts/scale_harness/fp17_change_burst_latency.py` |
| The alert rules load, and none of them can never fire | Yes -- 7 groups and 24 rules, `promtool` clean, and a test that every rule names a metric something actually publishes | Yes -- a profile-gated Prometheus container in the local stack scrapes the API target | **Partial** -- the thresholds needing an operator's number ship as documented placeholders | **Partial** -- config loaded, rules healthy and three alerts firing correctly; but **12 of the 24 rules have no data**, because `prometheus_client`'s registry is per-process and only `aida.main` serves `/metrics`, so the scheduler's footprint gauges are unreachable until an operator sets `AIDA_WORKER_METRICS_PORT`. Unreachable by design, not by defect, and deliberately not set here -- opening a port is the operator's decision. No alert has fired on a chosen threshold | 2026-09-17 | Platform lead | `infra/monitoring/`; `tests/test_monitoring_rules.py` |
| The shipped bootstrap produces a config that loads | Yes -- the dotenv source applies the same fuzzy rule the env path already used, so an operator-named credential reference is dropped while a near-miss of a real setting is still refused | Yes -- `get_settings()`, which every host-side script calls | Yes -- no setting required | **Yes** -- `cp .env.example .env` and load it as a dotenv file, which is the documented bootstrap and what nothing had tested: the new test fails with `extra_forbidden` against the previous code and passes after, and the typo guard passes both ways | 2026-09-17 | Platform lead | `src/atlas/platform/config.py`; `tests/test_config.py` |

## September 18: GraphQL reads, stored knowledge bundles, decidable triggers

Same column rules, and `Verified` stays strict -- a green test against in-memory SQLite is
**No**, not Yes. The stack was rebuilt and redeployed at the end of this cycle (migrations
`c5e9a2f7d314` and `e3a9c7d51f02` applied, parity 8 matched, 0 drifted). Status lives in
section P -- R11-FP01, FP13, GQL01, OKF02 and the new R11-D19 to R11-D22.

| Capability / property | Implemented | Reachable | Configured | Verified | Measured | Owner | Evidence |
|---|---|---|---|---|---|---|---|
| A trigger's parsed lineage can be decided, reaches impact analysis, and is re-examined when its function changes | Yes -- a TRIGGER entry in the one parsed-lineage review path, so the same queue, single decision, maker-checker and audit apply; ACTIVE edges join the unified graph as `TRIGGER_DEFINITION`; a per-trigger parse-coverage record; re-examination derived from the `ROUTINE` signal the function's redefinition already records, rather than a new subject kind | Yes -- the existing review routes and screen, the unified-lineage builder, and the lineage agent's pass | Yes -- no setting required | **Partial** -- the source side is live on both engines: a real trigger's edge is proposed, self-approval is refused, a person approves it and impact from the firing table reaches the audit table, and on PostgreSQL a real `CREATE OR REPLACE FUNCTION` leaves the trigger row unchanged and still triggers re-examination. The platform store in those tests is in-memory SQLite, and no deployed estate has decided a trigger edge | 2026-09-18 | Metadata lead | `src/aida/parsed_lineage_review_service.py`; `migrations/versions/c5e9a2f7d314_r11fp01_trigger_parse_coverage.py`; `tests/test_trigger_lineage_decidable.py`; `tests/test_trigger_lineage_live.py` |
| An aliased firing-row reference in a trigger resolves to the firing table | Yes -- the shared SELECT extractor fills in the caller's alias bindings instead of overwriting them, and the MERGE extractor now passes its binding on. It had recorded the source as a table literally named `inserted` | Yes -- every trigger parse | Yes -- no setting required | **Partial** -- five statement shapes fail against the old parser and pass now, and 489 existing lineage tests pass unchanged; proven on SQL text, not on a customer's trigger bodies | 2026-09-18 | Metadata lead | `src/aida/sql_lineage_parser.py`; `src/aida/procedure_lineage.py`; `tests/test_trigger_lineage.py` |
| The answer evaluation has the governed estate its corpus was written for | Yes -- `scripts/seed_governed_enrichment.py` publishes the ontology concept and alias with an approved mapping, takes a routine's discovered lineage to ACTIVE and approves its description, each proposed by one identity and decided by another through the platform's own decision paths | Yes -- a CLI against the running stack; `--same-identity` demonstrates the refusals | **Partial** -- thresholds are proposed in the baseline for the domain owner's sign-off and every `minimum` is still unset | **Partial** -- the estate was seeded live on the running stack, and a rerun changed nothing across all 193 sample-bank tables. **The evaluation itself has not been run**: it needs paid model calls the user has not authorised | 2026-09-18 | AI quality lead | `scripts/seed_governed_enrichment.py`, deciding through `src/aida/ontology_api.py`, `src/aida/parsed_lineage_review_api.py` and `src/aida/routine_description_api.py`; `tests/test_seed_governed_enrichment.py`; `Docs/90-reference/answer-evaluation-baseline.json` |
| The offline answer evaluation reaches no provider | Yes -- the CLI forces the embedding provider off on its offline branch. It had printed "no provider, no network" while embedding each candidate through a provider configured in `.env` | Yes -- the default mode of `scripts/answer_evaluation_benchmark.py` | Yes -- no setting required | **Partial** -- a test drives `main()` with a provider configured and fails against the old harness; no network capture was taken | 2026-09-18 | AI quality lead | `scripts/answer_evaluation_benchmark.py`; `tests/test_answer_evaluation_gate.py` |
| Catalog metadata is readable through GraphQL under exactly REST's authorization | Yes -- `POST /graphql`, typed and read-only, with pre-execution limits on depth, aliases, fragments, tokens and operation shape that run before the library parses; strawberry 0.327.7, chosen by a spike in which the alternative overflowed on a fragment cycle | Yes -- mounted in the API, listed in the surface-control matrix, with a versioned schema artifact and its own `--check` gate | **Partial** -- the limits are code constants, not yet `Settings`, and are not calibrated from telemetry | **Partial** -- REST/GraphQL parity (objects, order, pages, cursors, totals, refusals) and 23 refused abusive documents running zero statements are proven in-process against SQLite. On the redeployed stack, a typed query answers and the operation-name, mutation and no-principal refusals were confirmed live; no parity run against the deployed PostgreSQL | 2026-09-18 | Platform lead | `src/aida/graphql_api.py`; `src/aida/graphql_limits.py`; `src/aida/graphql_reads.py`; `tests/test_graphql_api.py`; `Docs/90-reference/graphql-schema.graphql` |
| A knowledge bundle is stored immutably, served as published, and refreshed only where something changed | Yes -- immutable publications with their documents and a compare-and-swap head per product version and caller authority; an incremental rebuild is byte-identical to a full export, and a no-op signal publishes nothing | Yes -- REST and MCP read the same stored publication; the knowledge view in Context Products and the evidence pane | Yes -- no setting required | **Partial** -- OKF-C was proven live on the running stack: approving one description changed exactly four of fifteen documents and a pinned download kept its digest. Those tables were created ahead of the migration and were then dropped, so the migration has been applied by redeploy but no bundle has yet been published through the migrated schema | 2026-09-18 | Semantics lead | `src/aida/okf_store.py`; `migrations/versions/e3a9c7d51f02_r11okf02_okf_bundle_store.py`; `tests/test_okf_store.py` |
| The catalog listing's total counts only tables the caller may read | Yes -- the count runs over datasources the gate admits, sharing one decision per datasource with the page filter. It had counted the whole organization, disclosing the size of sources the caller is refused | Yes -- `GET /v1/organizations/{organization_id}/catalog-rows` | Yes -- no setting required | **No** -- in-memory SQLite only; the test asserts the corrected total where the old code returned the refused rows too | 2026-09-18 | Platform lead | `src/atlas/modules/catalog/router.py`; `tests/test_catalog_rows_read_model.py` |
| A wide object is split into documents a reader can hold | Yes -- over `MAX_COLUMNS_PER_DOCUMENT` (100) columns, the object keeps its own document with every column name, and its rows move into `Atlas Column Set` documents split by ordinal range, so an appended or dropped column rewrites only its own set; concept documents carry their approved aliases | Yes -- every bundle render and incremental rebuild; profile `atlas-okf-export/3` re-renders stored bundles as a renderer change | Yes -- no setting required | **Partial** -- splitting, set stability and incremental-equals-full are proven over rendered bundles; no live estate holds an object over 100 columns, so no column set has yet been rendered from real data | 2026-09-18 | Semantics lead | `src/aida/okf_export.py`; `src/aida/okf_snapshot.py`; `tests/test_okf_context.py` |
| A question gets the bundle sections it needs, with exact receipts | Yes -- ranked from the publication's frozen snapshot with no model call, one hop of links, sections cut to a budget, long schema tables cut to the rows the question names, `NO_MATCH` rather than a guess | Yes -- `POST /v1/context-product-versions/{version_id}/okf-bundle/context`, the MCP tool `atlas__get_knowledge_context`, and the Knowledge view's preview, all through `read_published_bundle` | Yes -- budgets are settings, `okf_context_default_max_chars` (16,000) and `okf_context_ask_max_chars` (8,000), under a fixed 48,000 ceiling | **Yes** -- on the redeployed stack (parity 8 matched, 0 drifted), a `banking_context` product published through maker-checker answered four questions over REST and MCP from one publication: "closing position" reached the concept by its alias and its mapped table by one hop; a lineage question followed the table's dependency to the settlement routine; a payment question returned the payments schema; a weather question was `NO_MATCH`. The 15-minute revalidation then republished the fixed concept document and the log and carried the rest | 2026-09-18 | Semantics lead | `src/aida/okf_context.py`; `src/aida/okf_store.py`; `src/aida/okf_export_api.py`; `src/aida/mcp_server.py`; `tests/test_okf_context.py` |
| Ask generation is grounded in the product's approved knowledge | Yes -- asked through a context product, the SQL-generation payload carries the selected sections, screened like other free text; identifiers must still come from the metadata context; the run keeps each section's receipt | Yes -- `GovernedAgentOrchestrator._generate_statement` | Yes -- no setting required | **No** -- proven with a recording fake model in-process; no live model call has been made with it, and **whether it improves answers is unmeasured**: the before/after evaluation needs paid calls the user has not authorised | 2026-09-18 | AI quality lead | `src/aida/agent_orchestrator.py`; `tests/test_okf_context.py` |
| An Ask run's evidence after the plan stage is stored | Yes -- `flag_modified` on every publish, so model-call, budget, lineage and knowledge evidence reach the stored row | Yes -- every Ask run | Yes -- no setting required | **No** -- the stored column is read back in in-memory SQLite; the fix is ORM bookkeeping independent of the engine, but no deployed run has been read back | 2026-09-18 | Platform lead | `src/aida/orchestration_stages.py`; `tests/test_okf_context.py` |
| Ask asks which subject was meant when the product's knowledge names two equally | Yes -- a real tie between two subjects of one kind stops the run before any model call with a 409 `AMBIGUOUS_KNOWLEDGE` and both candidates; a name the question gives whole outranks one it only half gives, so a prefix is not a tie | Yes -- `POST /v1/datasources/{datasource_id}/agent-analyses` through a context product | Yes -- no setting required | **No** -- in-process with a recording fake model on in-memory SQLite, and the API mapping with the orchestrator patched; the Ask screen offers each candidate as a choice that asks again naming it (component test) | 2026-09-18 | AI quality lead | `src/aida/agent_orchestrator.py`; `src/aida/api.py`; `src/aida/okf_context.py`; `tests/test_okf_context.py`; `tests/test_ask_through_context_product.py` |
| Decided routine and trigger lineage follows the body | Yes -- after a complete re-parse, approved edges the body no longer writes are SUPERSEDED; superseded edges it writes again are PROPOSED for review; rejections stand. A redefined routine that keeps lineage is re-examined instead of failing (R11-D21) | Yes -- the lineage agent's routine and trigger paths and a person's parse under `require_review` | Yes -- no setting required | **No** -- in-memory SQLite; four mutants each caught, but no live source has been redefined under it | 2026-09-18 | Metadata lead | `src/aida/routine_lineage_edges.py`; `src/aida/lineage_agent.py`; `tests/test_decided_lineage_reconciliation.py` |
| A trigger's parse coverage is readable | Yes -- the stored coverage record, with the function the body was read from; 404 when no parse has measured it | Yes -- `GET /v1/datasources/{datasource_id}/triggers/{trigger_id}/parse-coverage` | Yes -- no setting required | **Yes** -- on the redeployed stack: 404 before any parse, then, after the lineage agent's trigger run through the API, the stored record for the sample bank's `trg_balance_load_audit` -- complete, 2 statements, naming its PostgreSQL function | 2026-09-18 | Metadata lead | `src/aida/procedure_lineage_api.py`; `tests/test_trigger_parse_coverage_route.py` |
| SQL a person reviews runs only when they press Run -- once, and only as validated | Yes -- `POST /v1/datasources/{datasource_id}/sql-drafts` drafts from a question (the governed Ask stages, stopping at the SQL) or takes a pasted statement, and validates it through the gateway without executing; a valid statement gets an expiring receipt binding a digest of the exact text, row limit, context product version and workspace to the caller, storing only the redacted shape. `POST /v1/sql-drafts/{receipt_id}/run` refuses someone else's, a spent, an expired or an edited statement, resolves the product again, claims the receipt by conditional update and then authorizes and validates in full | Yes -- both routes, and Ask's Review SQL first workspace | Yes -- migration `f4b8d2a6c913` applied; `sql_draft_receipt_ttl_minutes` defaults to 15 | **Yes** for pasted SQL -- on the deployed stack against Northwind's live PostgreSQL and SQL Server sources (32 of 32 checks: zero executions before Run, edit 409, cross-user 403, one execution, second Run 409, forbidden SQL unreceipted, no literal in receipt or audit), and the browser journey walked on the deployed UI. **No** for model-drafted SQL, which is proven only with a recording fake model: a live draft needs paid model calls | 2026-09-18 | Query gateway lead | `src/aida/sql_workspace.py`; `src/aida/sql_workspace_api.py`; `src/aida/agent_orchestrator.py` (`draft`); `ui-next/src/components/SqlWorkspace.tsx`; `tests/test_r11_sql01_sql_workspace.py`; `tests/test_footprint_journey.py` |
| A knowledge bundle's catalog labels are literal text | Yes -- names, types, classifications, signatures and table cells are escaped when written (profile `4`), and the publish policy treats an escaped character as text, so a hostile identifier can neither open a link nor split a table | Yes -- every bundle write | Yes -- no setting required | **No** -- in-process on in-memory SQLite, rendered with markdown-it; not re-measured against a live source's hostile identifiers | 2026-09-18 | Context lead | `src/aida/okf_export.py`; `tests/test_okf_markdown_safety.py`; `Docs/90-reference/okf-export-profile.md` |
| A GraphQL mutation executes an approved tool once per caller-scoped key, through the governed tool path | Yes -- `executeGovernedTool` calls `tool_api.execute_tool_version` (roles, contract, quality holds, masking, cost gate, audit) with an optional product scope; a durable `governed_execution_request` row claimed before execution makes a duplicate a replay, key reuse a conflict and an unknown outcome PENDING; admission allows one execution field per mutation | Yes -- `POST /graphql`, mutation `executeGovernedTool` and query `governedExecution` | Yes -- migration `a5d1c8e3f7b2` applied on the deployed stack; row cap 1,000; mutation deadline is the statement timeout plus 15 seconds | **Yes** -- on the deployed stack (`d2aae14`, parity 8 matched, 0 drifted) against Northwind's live PostgreSQL source: one execution with rows, replay executing nothing, key reuse refused, receipt ownership, one audit record, two roots refused; SQL Server through the live footprint journey | 2026-09-18 | Runtime + API lead | `src/aida/governed_execution.py`; `src/aida/graphql_schema.py`; `src/aida/graphql_limits.py`; `tests/test_r11_gql02_graphql_execution.py`; `tests/test_footprint_journey.py` |

## September 19: token-grain lineage, PL/SQL calls, OKF import, batch review

Same column rules; `Verified` stays strict. Everything below was verified on in-memory SQLite or
against scratch PostgreSQL for migrations only, so no row says Yes. Status lives in section P --
R11-FP03, FP07, OKF03, REV01, D17, GQL01 and the new R11-D28 and R11-D29.

| Capability / property | Implemented | Reachable | Configured | Verified | Measured | Owner | Evidence |
|---|---|---|---|---|---|---|---|
| A routine or trigger lineage edge points at the token that is its evidence | Yes -- `source_token_range` and `target_token_range` beside the statement range, indexing the stored redacted body; kept only when every identifier in the statement slices out of the stored text unchanged, and only when exactly one reference can be the evidence -- otherwise NULL, never the first occurrence | Yes -- every routine and trigger parse, and both deep-lineage read routes | Yes -- no setting required | **Partial** -- migration `cd1d15b6705b` up, down, up and the ORM drift test on scratch PostgreSQL; the parse itself on unit fixtures only | No | Engineering | `src/aida/procedure_token_ranges.py`; `tests/test_r11_fp07_token_ranges.py` |
| A PL/SQL body's calls, sibling-member calls and declarations are read | Yes -- a bare call is a named `NESTED_PROCEDURE_CALL` gap that catalog descent reads through; a sibling member resolves by arity, named arguments and defaults, `(AMBIGUOUS)` when overloads differ only by type; `CURSOR ... IS` declarations are located reads; `%TYPE`/`%ROWTYPE` are deliberately not reads | Yes -- every Oracle routine and package parse, and the lineage agent, which now passes member routine ids | Yes -- no setting required | **No** -- unit fixtures only; no live Oracle | No | Metadata lead | `src/aida/procedure_lineage.py`; `tests/test_r11_fp03_member_calls_and_declarations.py` |
| An unqualified column is attributed to the table that owns it in its scope | Yes -- resolved at its own query level and the outer levels it can correlate to, with the statement's own target in scope; two candidate owners read `UNRESOLVED`; declared variables, cursor parameters, record fields, sequences and `EXCLUDED` are not tables. It had recorded `DELETE FROM dbo.final WHERE id IN (SELECT r.id FROM dbo.rejects r)` as a read of `dbo.rejects.id` | Yes -- every routine parse | Yes -- no setting required | **No** -- unit fixtures only | No | Engineering | `src/aida/procedure_column_owners.py`; `tests/test_lineage_attribution_and_cursor_reads.py` |
| A cursor's query is read wherever it is written | Yes -- `OPEN c FOR <query>`, T-SQL `DECLARE c CURSOR FOR` and `SET @c = CURSOR FOR`, and PL/pgSQL cursor and query-default declarations read into routine-local state; `OPEN ... FOR <string>` and `FOR EXECUTE` are dynamic SQL. All three had been dropped without a gap | Yes -- every routine parse | Yes -- no setting required | **No** -- unit fixtures only | No | Engineering | `tests/test_lineage_attribution_and_cursor_reads.py`; `Docs/90-reference/procedure-lineage-capability-matrix.md` |
| An edited knowledge bundle becomes pending proposals, never direct writes | Yes -- preview then apply-the-accepted-preview; descriptions land as one `OKF_IMPORT_BATCH` model-import batch per datasource, pinned at T2, and concept edits as a `PENDING_APPROVAL` ontology version; imported status grants nothing; archive, YAML and link limits refuse with named reason codes | Yes -- `POST /v1/context-product-versions/{version_id}/okf-bundle/imports/preview` and `.../okf-bundle/imports` | **No** -- `okf_import_enabled` ships off by design until the row's acceptance; both routes answer 403 while it is off | **No** -- in-memory SQLite: 15 journey tests and 89 hostile-content tests | No | Ingestion + Security + Semantics | `src/aida/okf_import_bundle.py`; `src/aida/okf_import.py`; [contract](../90-reference/okf-import-contract.md); `tests/test_okf_import.py`; `tests/test_okf_import_hostile.py` |
| A reviewer can decide a large frozen selection, and each member is re-checked | Yes -- a keyset-paged change queue; a batch freezes ids with the evidence fingerprint shown; deciding re-checks every member and runs each through `decide_review`, the path single and bulk decisions use, reporting per-member outcomes with reason codes; agents refused | Yes -- `/v1/governance/reviews/change-queue` and `/v1/governance/review-batches` routes; a Batch review tab on the review screen | Yes -- no setting required | **No** -- in-memory SQLite; the claim race is simulated there | **Partial** -- on SQLite: 3 statements per 100-row page at 10 and 2,000 queued; 1,000 approvals decided in 12.8s at about 14 statements each. No PostgreSQL timings | Governance + Frontend | `src/aida/review_batches.py`; `tests/test_review_batches.py`; `tests/test_review_batches_scale.py`; `ReviewBatchQueue.test.tsx` |
| A playbook's rule can be previewed without applying it | Yes -- a dry run writes nothing and reports, per action, that TAG, CLASSIFY, OWN and CERTIFY auto-apply with no human when the match count is within `auto_apply_max_items`, and that an auto-applied run has no governed reversal | Yes -- `GET /v1/playbooks/{playbook_id}/dry-run` | Yes -- no setting required | **No** -- in-memory SQLite; results are not stored or bound to a later run | No | Governance | `src/aida/playbook_dry_run.py`; `tests/test_playbook_dry_run.py` |
| Lineage is readable through GraphQL with REST's traversal, under the catalog's workspace gate | Yes -- `lineageImpact` and `lineageGraph` call the builders the unified-lineage routes call (`aida.unified_lineage_service`, moved out of the router unchanged) with the routes' bounds, review filter, cache and graph-store settings; both directions, the nodes and the edges page by cursor; the datasource's workspace gate is asked, as the REST routes and MCP tools now do too (R11-D28) | Yes -- `POST /graphql`; six resolver rows in the surface-control matrix, each with the lineage reader roles, tenant and workspace checks | Yes -- no setting of its own | **Partial** -- on the stack deployed at `de84027`, every page of both directions for two focus tables and the whole graph's nodes and edges equal REST's, against PostgreSQL. The four review filters, the refusals and the admission price are proven in-process against SQLite only | 2026-09-19 | Platform lead | `src/aida/unified_lineage_service.py`; `src/aida/graphql_reads.py`; `src/aida/graphql_schema.py`; `tests/test_graphql_lineage.py` |
| A lineage read is refused to a caller the datasource's workspace refuses, on every surface | Yes -- the unified-lineage graph and impact routes, the impact export, the domain graph (per contributing datasource, withheld and counted), the MCP lineage tools and GraphQL each ask `READ_METADATA` on the datasource, as its catalog reads do | Yes -- the surface-control matrix shows workspace `yes` on the four routes | Yes -- enforced only where a workspace is ENFORCE; the default SHADOW posture allows and records | **Partial** -- on the stack deployed at `de84027` the routes read as before and the domain graph reports 0 withheld; the refusal and member admission are proven in-process against SQLite only, since the dev estate has no enforcing workspace | 2026-09-19 | Security lead | `src/aida/unified_lineage_api.py`; `src/aida/lineage_evidence_export_api.py`; `src/aida/mcp_server.py`; `ui-next/src/screens/UnifiedLineageScreen.tsx`; `tests/test_lineage_workspace_gate.py` |

## September 19 (afternoon): workspace-gate follow-through, typed SQL parameters, source bundles, batch resume

Same column rules; `Verified` stays strict. Each change below was integrated as its own commit, with its tests and generated artifacts run on a clean worktree of the previous commit plus that change only. Status lives in section P -- R11-D30, S13, SQL01, FP07, FP03, OKF01, OKF02, OKF03, REV01 and GQL01 -- and R11-C14 records that connector capability flags are still hand-declared.

| Capability / property | Implemented | Reachable | Configured | Verified | Measured | Owner | Evidence |
|---|---|---|---|---|---|---|---|
| Routine and trigger lineage and parse coverage are refused to a caller the datasource's workspace refuses | Yes -- `procedure_lineage_api._load_readable_datasource` asks `gate_read` (`READ_METADATA` on the datasource) before the routine or trigger is looked up, on the parse, lineage, routine parse-coverage and trigger parse-coverage routes. **Corrects the September 19 row above**, whose "on every surface" missed these four routes (R11-D30) | Yes -- the surface-control matrix shows workspace `yes` on all four routes | Yes -- enforced only where a workspace is ENFORCE; the default SHADOW posture allows and records | **Partial** -- on the stack deployed at `c340c50` the routes serve as before under SHADOW; the refusal is proven in-process on SQLite only (`tests/test_procedure_lineage_workspace_gate.py`, 12; removing the gate fails exactly the 4 refusal tests), since the dev estate has no enforcing workspace | 2026-09-19 | Security lead | `src/aida/procedure_lineage_api.py`; `tests/test_procedure_lineage_workspace_gate.py` |
| Stewardship is one workspace: Work queue, Bulk actions, Automation | Yes -- `StewardshipWorkspace` on `?view=`; `#/playbooks` is a retired alias to the Automation view; pre-workspace bulk links keep their filter; unsaved-change and ARIA tab keyboard behaviour; no endpoint, role or control enablement changed (R11-S13) | Yes -- `#/steward/stewardship` | Yes -- no setting required | **Partial** -- on the stack deployed at `c340c50` `#/playbooks` opens the Automation view and it survives a reload; the rest is jsdom only (162 tests, ten mutations caught), with no keyboard or full browser acceptance | 2026-09-19 | Frontend lead | `ui-next/src/screens/StewardshipWorkspace.tsx`; `src/aida/playbooks_api.py` (Automation); `src/atlas/modules/catalog/router.py` (the four `bulk-*` actions); `Docs/10-architecture/23-stewardship-action-map.md` |
| A pasted SQL statement carries typed parameter values, bound as the governed tools bind them | Yes -- `parameters` (name, governed-tool type, value) on `sql-drafts` and `run`, bound by `render_tool_sql` as one escaped typed literal per placeholder node; the receipt digest covers types and a keyed `sign_value` fingerprint, so a changed value at Run is 409; no value is stored; an undeclared placeholder is refused before the gateway. Not driver-level binding: the gateway and connectors take SQL text only (R11-SQL01) | Yes -- the SQL workspace in Ask and its Parameters editor | Yes -- no setting required | **Partial** -- through the API of the stack deployed at `c340c50`: a parameterised SELECT ran (1 row), an injection value returned 0 rows, a changed value was 409 `REVALIDATION_REQUIRED` without consuming the receipt; `tests/test_r11_sql01_parameters_live.py` on real PostgreSQL and SQL Server; the Parameters editor was not walked through in a browser | 2026-09-19 | Query gateway lead | `src/aida/sql_workspace.py`; `ui-next/src/components/SqlWorkspaceParameters.tsx`; `tests/test_r11_sql01_parameters.py` |
| A cursor loop's rows, a returned ref cursor and a column-less read are lineage | Yes -- a cursor FOR loop reads into routine-local `<LOCAL:rec@N>` state that `rec.col` reads inside the loop; `OPEN c FOR q` on a cursor handed back to the caller is the result set; a table read without a named column gets a `TABLE_ROWS` edge; query-bearing conditions are read (R11-FP07, R11-FP03) | Yes -- every routine and trigger parse | Yes -- no setting required | **No** -- unit fixtures (44 new tests, 13 of 13 mutants); live lineage tests not re-run | 2026-09-19 | Engineering | `src/aida/procedure_lineage.py`; `tests/test_r11_fp07_loop_records_and_result_cursors.py` |
| A datasource has its own stored knowledge bundle, through the same store as a product's | Yes -- a `DATASOURCE`-scope lineage in the existing publication and head tables (migration `f7c2d9a4b61e`), served by the product store's shared `_serve`/`_publish`; `READ_METADATA` on the datasource and per schema when a workspace decides; five routes under `/v1/datasources/{id}/okf-bundle`; concepts and tools excluded (R11-OKF01/OKF02) | Yes -- REST and the Sources screen (`?knowledge=1`); no MCP or GraphQL door yet | Yes -- no setting required | **Partial** -- on the stack deployed at `c340c50` the Customer Master bundle published 23 documents and its context route answered `MATCHED`; revocation and incremental rebuild are proven in-process (22 tests, 12 of 12 mutants) and on a private PostgreSQL database only | 2026-09-19 | Context lead | `src/aida/okf_store.py`; `src/aida/okf_export_api.py`; `tests/test_okf_source_bundles.py` |
| An edited routine purpose imports as a pending routine description, and an import review shows what approving does | Yes -- `OKF_IMPORT_ROUTINE_DESCRIPTION` reviews publish only through `apply_routine_description_draft`, refused first where that workflow would refuse (including a body changed since export); `GET /v1/governance/reviews/{id}/okf-import-preview` and `OkfImportReviewPreview` in the review queue (R11-OKF03) | Yes -- the review queue; import itself only when `okf_import_enabled` | **No** -- `okf_import_enabled` is off pending the owner's acceptance | **No** -- in-process, plus 38 tests on a private PostgreSQL database | 2026-09-19 | Context lead | `src/aida/okf_import.py`; `src/aida/okf_import_review.py`; `tests/test_okf_import_routines.py`; `tests/test_okf_import_postgres.py` |
| Batch approval checks each type's evidence, and an interrupted batch decision resumes | Yes -- `BATCH_APPROVAL_EVIDENCE` per object type (`REQUIRED_EVIDENCE_MISSING`, reject-only `NO_EVIDENCE_CONTRACT`); decisions commit per 100 members with per-member savepoints and resume without re-deciding; stored playbook dry runs bind a later run (migration `92b060bb2e10`) (R11-REV01) | Yes -- `/v1/governance/review-batches`, `/v1/playbooks/{id}/dry-runs`, the batch queue and the Automation view | Yes -- no setting required | **No** -- in-process, plus concurrency and resume on a private PostgreSQL database | 2026-09-19 -- deciding 1,000 members 27.1-33.1s at 15.05 statements per member on PostgreSQL | Governance lead | `src/aida/review_batches.py`; `src/aida/playbook_dry_run.py`; `tests/test_review_batches_postgres.py` |
| GraphQL serves context-product and parse coverage, with worked examples, an introspection policy and limits as settings | Yes -- `contextProductCoverage` decided as the compile route decides; `routineParseCoverage`/`triggerParseCoverage` as their REST routes, workspace gate included; ten examples executed by a test; `graphql_*` limit settings; introspection off by default and role-limited when on (R11-GQL01) | Yes -- `POST /graphql` | Yes -- limits default to the prior values; budgets need `AIDA_GRAPHQL_BUDGET_ENABLED` and the cache profile | **Partial** -- on the stack deployed at `c340c50` with budgets on: 120 requests a minute then 429 `RATE_LIMITED`, both parse and product coverage answered, introspection refused; the introspection role policy and the examples are proven in-process (47 + 27 + 13 tests, 18 of 18 mutants) | 2026-09-19 | API lead | `src/aida/graphql_reads.py`; `Docs/90-reference/graphql-examples.md`; `tests/test_graphql_coverage.py` |

## Why this document exists

D06 found documentation that had drifted from the code, and the review named the cause rather than
just the symptom:

> Use one current register separating implemented, reachable, configured, and verified, with
> dates/owners/evidence. Accomplishment logs are history, not current state.

The four words are not synonyms, and collapsing them is how a project ends up believing it has a
capability it does not have. This register keeps them apart on purpose.

| Column | Question it answers | What does **not** earn a Yes |
|---|---|---|
| **Implemented** | Does committed source in this repository do the thing? | A schema, a settings field, a TODO, or a function that logs and returns success |
| **Reachable** | Does a process that actually runs invoke it? A mounted route, a `compose.yaml` service command, a Temporal worker/scheduler registration, or a CLI entry point | Being importable. Being covered by a unit test. Being called only from another unreachable module |
| **Configured** | Do the settings, credentials and destinations this repository *ships* let it do its job in the local topology? | A settings field that exists but defaults to off, empty, or a placeholder. "The bank will supply it" is a legitimate **No** |
| **Verified** | Has someone run it end-to-end against the real destination or the real topology and left evidence? | A green unit test against a mock or an in-memory SQLite. A local success object. A tracker row saying DONE |

**Verified is the strict one.** The review is explicit that a local success object is not
destination evidence, so almost every external-integration row below says **No**. That is the
honest answer, and a row that says No here is more useful than a row that says Yes and is wrong.

**Scope of the last pass.** Every row was re-measured on 2026-09-12 by reading the code named in
its Evidence column against the tree, and — where it could be done without infrastructure this
repository does not have — by running it. Four rows keep an earlier date on purpose; the banner at
the top of this file names them. The rows that the earlier remediation pass left *under
remediation* now describe the landed implementation.

Note what did and did not move. `Implemented` and `Reachable` changed for several rows.
`Verified` did **not** change for a single external integration, because nothing in that pass
could contact a real destination: an archive that reads its own bytes back off a local filesystem
and a webhook delivered to a loopback server are evidence of a working implementation, not of a
production receipt. That distinction is the entire reason this register has four columns instead
of one, and it is why F01 and F04 are recorded as implemented-and-unverified rather than done.

## Legend

* **Yes** — holds today, with the evidence cited.
* **Partial** — holds for a named slice; the row says which.
* **No** — does not hold. Not a defect by itself; often a deliberate default-off posture.
* **n/a** — the column does not apply (e.g. nothing to configure).

Owner values name the area, not a person: this repository has no CODEOWNERS file yet, and
inventing individual names would be its own truth drift.

## Register

### Platform and runtime

| Capability | Implemented | Reachable | Configured | Verified | Date | Owner | Evidence |
|---|:--:|:--:|:--:|:--:|---|---|---|
| REST API (FastAPI modular monolith) | Yes | Yes | Yes | Partial — local Compose only, but exercised there on 2026-09-12: the running stack answered `GET /health/ready` with `status: UP` (`postgresql` UP, `temporal` UP, `outbox_backlog` UP, `audit_archive_task` running), service `aida-control-plane` 1.2.0. No non-local topology, no load or failover certification | 2026-09-12 | Platform | `src/aida/main.py`; `compose.yaml` `api` service with a `/health/live` healthcheck; CI `docker-build` job imports `aida.main` from the built image |
| Backend container image | Yes | Yes | Yes | Yes — the image built from this `Dockerfile` is running in the local stack and answering `/health/ready` (checked 2026-09-12); CI `docker-build` imports `aida.main` from the built image | 2026-09-12 | Platform | `Dockerfile`; CI `docker-build` job; `scripts/check_image_packaging.py` |
| Public Tool SDK shipped in the image | Yes | n/a — a client library, not a server path | Yes | Yes — re-run 2026-09-12: `docker exec … python -c "import aida_tool_sdk"` inside the running `api` container imported it and reported 0.1.0 | 2026-09-12 | Platform | `sdk/aida_tool_sdk/`; `pyproject.toml` `[tool.hatch.build.targets.wheel].packages`; `Dockerfile` `COPY sdk ./sdk`; `scripts/check_image_packaging.py` |
| Alembic migrations against real PostgreSQL | Yes | Yes | Yes | Yes — CI `migration-drift` applies every migration to an empty Postgres 16 and diffs against `Base.metadata`. **CI is the evidence, not the local stack:** the long-lived local development database was found at revision `d41a7b8e6c02` on 2026-09-12, behind the tree's six heads, so the running stack's schema is not this tree's | 2026-09-12 | Platform | `migrations/`; CI `migration-drift` job, `tests/test_migration_orm_drift.py` |
| Temporal durable workflows | Yes | Yes | Yes | Partial — `temporal` reported UP through `/health/ready` in the running local stack on 2026-09-12, and the worker registers `DatasourceDiscoveryWorkflow` and `MetadataBatchIngestionWorkflow`. Still local Compose only; no failover, no continue-as-new at scale | 2026-09-12 | Platform | `compose.yaml` `temporal`, `metadata-worker`, `fleet-scheduler`; `src/aida/workflows/worker.py`; `src/aida/workflows/` |
| Kafka/Redpanda outbox publication | Yes | Yes | **No by default since 2026-09-12** — R11-X9 moved `redpanda`/`outbox-publisher` behind the `events` (and `graph`) Compose profile, because with default settings nothing consumed the topics. Producers still write `outbox_event` rows, which stay `PENDING` until a publisher runs | Partial — local Compose only, and only under a profile; no database↔Kafka atomicity, per REVIEW §5 | 2026-09-12 | Platform | `compose.yaml` profiles; `src/aida/projectors/outbox_publisher.py`; README step 2 |
| Neo4j graph projection | Yes | Yes | **No by default since 2026-09-12** — R11-X9 moved `neo4j`/`graph-projector` behind the `graph` profile; the default graph backend is `postgres`, which is the certified one. R11-D8 also stopped reconciliation dialling Neo4j regardless of backend | **No** — INV-9 still records the Neo4j backend as uncertified; the projection-rebuild drill (E5) has never been run | 2026-09-12 | Platform | `compose.yaml` profiles; `src/aida/graph_store.py`; `src/aida/graph_reconciliation.py`; `00-status.md` §3 INV-9 |
| Disaster recovery / restore | No | No | No | **No** — needs a deployed topology and an approved RPO/RTO | 2026-09-12 | Bank decision | Re-checked 2026-09-12: `scripts/` holds no backup, restore or failover script (the only drill script is `scripts/agent_kill_switch_drill.py`, which is unrelated), and no runbook covers one. REVIEW §5 "Recovery"; POINTS-TRACKER T28 |

### Frontend and public endpoints

| Capability | Implemented | Reachable | Configured | Verified | Date | Owner | Evidence |
|---|:--:|:--:|:--:|:--:|---|---|---|
| Atlas portal (`ui-next`) — the only portal | Yes | Yes | Yes | Partial — more than the 2026-09-06 cell credited. CI runs `vite build`, `tsc --noEmit`, `vitest`, an axe-core WCAG 2.1 A/AA sweep of all 44 navigable screens with an accessible-name floor, and a six-step Playwright journey in real Chromium through the production nginx image. Two gaps remain: the journey's upstream is a stub synthesised from the OpenAPI baseline, not the real backend, and the human half of accessibility (screen reader, real-rendering contrast, 100% zoom) is unexecuted | 2026-09-12 | Frontend | `ui-next/`; `compose.yaml` `ui-next` on `:3001`; CI `ui-next`, `ui-journey` jobs; `ui-next/src/a11y-sweep.test.tsx`; `24-accessibility-acceptance-2026-09-12.md` |
| Legacy `ui/` portal | **Removed** | n/a | n/a | n/a | 2026-09-12 | — | `ls ui` returns nothing; no compose service, Dockerfile, CI job or script references it (re-checked 2026-09-12) |
| API reachable from the browser on one origin (`/v1/`) | Yes | Yes | Yes | Yes — CI `ui-proxy` exercises the nginx proxy against a stub upstream, and on 2026-09-12 a request through the running `ui-next` nginx on `:3001` reached the real API and was answered by it (the API's own `401 X-Principal-Id is required` refusal, then `200` once the header was supplied) | 2026-09-12 | Platform | `ui-next/nginx.conf`; `ui-next/vite.config.ts`; `scripts/check_proxy_contract.py`; CI `ui-proxy` job |
| MCP endpoint reachable at the URL the UI advertises (`/mcp`) | Yes | Yes | Yes | **Yes as of 2026-09-12** — the gap this row named is closed. A full JSON-RPC `initialize` through the deployed stack succeeded: `POST http://localhost:3001/mcp` (through the `ui-next` nginx, with `X-Principal-Id`) returned HTTP 200 with `protocolVersion 2025-03-26`, `serverInfo.name atlas-governed-data-platform` and the `tools`/`resources`/`prompts` capability set. The proxy hop was already verified | 2026-09-12 | Platform | F07; `src/aida/mcp_server.py` `APIRouter(prefix="/mcp")`; `ui-next/nginx.conf`; CI `ui-proxy` job |
| Browser OIDC sign-in flow | Yes — real JWT verification against a JWKS endpoint, with issuer/audience checks and role and persona mapping | Yes — `security.py` is on every request path and selects the OIDC verifier when `identity_provider == "oidc"` | Partial — **the 2026-09-06 cell's "no IdP is configured in this repository" is out of date.** `compose.oidc.yaml` (added 2026-09-10) ships a mock IdP — `mock-oauth2-server` — wired to `AIDA_OIDC_ISSUER`, `AIDA_OIDC_JWKS_URL`, `AIDA_OIDC_AUDIENCE` and role/persona mappings, with a `ui-next` image built against it. It is a development overlay and a *mock*: the default `compose.yaml` still runs development principal-header mode, and no real bank IdP is configured | **No** — the overlay was not brought up in this pass, so no sign-in has been driven through it | 2026-09-12 | Frontend | F06; `compose.oidc.yaml`; `src/aida/security.py`; `src/aida/oidc.py`; POINTS-TRACKER F06 |

### Connectors

Maturity values are read from `src/aida/connectors/registry.py`, which is authoritative. Six
database connectors are registered `BETA`; two are declared `PLANNED`.

| Capability | Implemented | Reachable | Configured | Verified | Date | Owner | Evidence |
|---|:--:|:--:|:--:|:--:|---|---|---|
| PostgreSQL connector (`BETA`) | Yes | Yes | Yes | Yes — discovery run against live Postgres 16 **and** 14 service containers in CI `connector-version-fixtures`; the `sample-source` Postgres fixture was also healthy in the running local stack on 2026-09-12 | 2026-09-12 | Connectors | `connectors/postgres.py`; CI `connector-version-fixtures` job, `tests/test_postgres_version_fixtures.py` |
| Microsoft SQL Server connector (`BETA`) | Yes | Yes | Yes | Partial — exercised against the `sample-mssql-source` Compose fixture, which was healthy in the running local stack on 2026-09-12; no multi-version, TLS or delegated-identity certification | 2026-09-12 | Connectors | `connectors/sqlserver.py`; `compose.yaml` `sample-mssql-source` |
| Oracle connector (`BETA`) | Yes | Yes | **No** — re-checked 2026-09-12: `compose.yaml` still defines no Oracle service (the sample source was retired) | **No** — never run against a live Oracle | 2026-09-12 | Connectors | `connectors/oracle.py`; registry notes: query estimate fails closed with `QUERY_ESTIMATE_UNAVAILABLE_FOR_CONNECTOR` |
| BigQuery connector (`BETA`) | Yes | Yes | **No** — needs a real GCP project | **No** — never run against a live BigQuery | 2026-09-12 | Connectors | `connectors/bigquery.py`; registry notes honestly omit foreign-key metadata |
| Snowflake connector (`BETA`) | Yes | Yes | **No** — needs a real account/warehouse | **No** — never run against a live Snowflake | 2026-09-12 | Connectors | `connectors/snowflake.py` |
| Databricks connector (`BETA`) | Yes | Yes | **No** — needs a real workspace | **No** — the registry note says so in the code: "Code complete; never exercised against a live Databricks workspace" | 2026-09-12 | Connectors | `connectors/databricks.py` |
| Teradata, IBM Db2 | **No** — canonical push ingestion only | n/a | n/a | n/a | 2026-09-12 | Connectors | `connectors/registry.py` `declare_planned(...)`, `implementation_status="PLANNED"` (re-read 2026-09-12: six connectors `BETA`, these two `PLANNED`) |

### Governance, audit and delivery

Every row in this section is the subject of an open P0/P1 review finding. They are listed with the
finding rather than omitted, because a capability register that quietly drops the contested rows is
the exact failure mode D06 describes.

| Capability | Implemented | Reachable | Configured | Verified | Date | Owner | Evidence |
|---|:--:|:--:|:--:|:--:|---|---|---|
| Audit ledger and transactional outbox | Yes | Yes | Partial — the ledger and the outbox *writes* are default-on, but *publication* is not: R11-X9 moved `outbox-publisher` behind the `events`/`graph` profile, so on a default stack `outbox_event` rows accumulate `PENDING`. See the Kafka/Redpanda row above | Partial — local Compose only, and no database↔Kafka atomicity (REVIEW §5). Measured live on 2026-09-12 with the profile enabled: 478 `audit_event` rows, 411 `outbox_event` rows, **0 PENDING**, and `/health/ready` reporting `outbox_backlog: UP` | 2026-09-12 | Audit | `src/aida/events.py`; `src/aida/projectors/outbox_publisher.py`; `compose.yaml` profiles |
| WORM audit archive to an external immutable store | Yes — two complete providers: `filesystem` (two-phase lifecycle, full-envelope versioned checksum, membership, lease) and `s3` over S3 Object Lock, signed by a standard-library SigV4 implementation with no SDK dependency; `gcs`/`azure_blob` still resolve to a provider that refuses | Yes — archive loop runs from `main.py`, which also creates the Object Lock bucket when the backend is `s3` | Yes — defaults to `none`, which refuses rather than reporting success; `s3` reads the existing `object_store_*` settings | Partial — **against MinIO, not AWS.** All four F01 properties were exercised against a live S3-compatible service on 2026-09-09: object retrieved and re-checksummed; a raw overwrite created a new version while the locked version stayed intact and re-verified; deleting the locked version before retain-until was refused by the service (MinIO: `400 InvalidRequest`, "Object is WORM protected"); a legal hold blocked deletion after retention had lapsed, and release restored expiry. Not yet exercised against a real AWS S3 bucket, where refusals are `403 AccessDenied` and IAM/bucket policy, not just Object Lock, is in play. Filesystem immutability remains a guard rail, not a security boundary | 2026-09-09 | Audit | F01/F02/F03; `src/aida/worm_archive.py`, `src/aida/audit_archive_storage.py`, `src/aida/audit_archive_s3.py`, `src/aida/aws_sigv4.py`, `src/aida/audit_envelope.py`, `tests/test_audit_archive_s3.py`, `tests/test_aws_sigv4.py` |
| SIEM / SOC event routing | Yes — real webhook and syslog transports (RFC 5424; RFC 6587 octet counting on TCP) behind durable delivery intents with retry, backoff and dedup | Yes — worker runs from the scheduler | Partial — re-confirmed against the shipped defaults on 2026-09-12: `siem_endpoint` is still the placeholder `internal://security-log-pipeline`, which `parse_siem_endpoint` resolves to NOT_CONFIGURED, and `delivery_worker_enabled` still defaults `False`, so an upgrade sends nothing | **No** — receipt is proven against loopback stub servers, not a real SOC collector | 2026-09-12 | Delivery | F04; `src/aida/siem_routing.py`, `siem_delivery.py`, `delivery_intents.py` |
| Governance notification delivery | Yes — intent created in the business transaction, delivered by an independent worker; requested/attempted/delivered timestamps separated | Yes — worker runs from the scheduler | Partial — default-off, as above (`delivery_worker_enabled=False`, re-confirmed 2026-09-12) | **No** — outage and recovery are exercised against a stub server, not a real channel | 2026-09-12 | Delivery | F12; `src/aida/governance_review_relay.py`, `governance_notifications.py`, `delivery_intents.py` |
| Maker-checker review (single decision) | Yes | Yes | Yes | **Yes as of 2026-09-12** — the contention gap this row named is closed. `tests/test_governance_decision_postgres_concurrency.py` races the single-decision router against a **real PostgreSQL** with separate sessions on separate connections released from a shared barrier, at READ COMMITTED *and* REPEATABLE READ; the loser blocks on the winner's `FOR UPDATE` row lock and is refused. Run on 2026-09-12: 14 passed (command in the appendix) | 2026-09-12 | Governance | `src/aida/semantic_api.py` single-decision path uses `SELECT ... FOR UPDATE`; `tests/test_governance_decision_postgres_concurrency.py` |
| Maker-checker review (bulk decision) | Yes — single, bulk, sample-review and reviewer-agent decisions share one service whose claim is a compare-and-set `UPDATE … WHERE status='PENDING'` | Yes | Yes | **Yes as of 2026-09-12** — the 2026-09-06 cell said "a concurrent PostgreSQL reproduction is still outstanding"; **it already existed on that date and the cell was wrong.** It was run on 2026-09-12 against real PostgreSQL — 14 passed — covering single-vs-single, single-vs-bulk, bulk-vs-bulk, the lock-free service claim, a three-way race and a bulk sweep against a lock-free decider, each at two isolation levels, with the falsifying assertion that a contended review enters its target's side-effect path exactly once. Recorded finding: at REPEATABLE READ the loser is refused as SQLSTATE 40001, not as 409 CONFLICT, so raising isolation needs a retry loop | 2026-09-12 | Governance | F05; `src/aida/governance_decision_service.py`; `tests/test_governance_decision_postgres_concurrency.py` |
| Workspace ABAC authorization | Yes | Yes | Yes | **No as an enforcing control** — confirmed live on 2026-09-12, which is stronger than the reading it replaces: `/health/ready` reports `workspace_authorization: OBSERVING` with `workspaces_total=0`, `workspaces_enforcing=0` and `unresolved_scope: PROCEEDS_UNDECIDED`. The system measures and does not deny, and there is not yet a single workspace to enforce over. Deliberate; see F11 | 2026-09-12 | Platform | F11; `src/atlas/platform/config.py`; `src/aida/authorization_gate.py`; `00-status.md` §3 INV-4 |
| Enterprise secret manager boundary | Yes | Yes | **No** — re-confirmed 2026-09-12: `Settings.reject_insecure_production_configuration` forbids `credential_provider == "env"` in production, and no bank provider is registered in this repository, so the production-valid path has no resolver behind it | **No** — no rotation or outage drill | 2026-09-12 | Platform | `src/aida/secrets.py`; `00-status.md` §4 "Enterprise secrets and source identity" |

### AI and analytics

| Capability | Implemented | Reachable | Configured | Verified | Date | Owner | Evidence |
|---|:--:|:--:|:--:|:--:|---|---|---|
| Query execution gateway (single choke point) | Yes | Yes | Yes | Partial — INV-2 is enforced by four layers as of 2026-09-12: the type system, an import contract, an AST scan for the executor's methods, and a scan for driver `connect` calls outside `aida.connectors`. The fourth was added because the first three could not see `policy_native_sync` dialling a source directly (R11-D1); that apply path is removed. Cancel propagation remains uncertified, and no live execution was performed in this pass | 2026-09-12 | Platform | ADR-0004; `tests/test_tier0_invariants.py`; CI `quality` job runs `lint-imports` |
| Deterministic prompt-risk screening and SQL guard | Yes | Yes | Yes | Partial — measured against a synthetic corpus (zero bypasses, zero false positives), not against real bank traffic; re-confirmed 2026-09-12 that CI `quality-baseline` still runs `scripts/quality_benchmark.py` on every push | 2026-09-12 | AI governance | `src/aida/injection_defense.py`; `Docs/90-reference/model-risk-benchmark-results.md`; CI `quality-baseline` job |
| Model generation (OpenAI / Gemini) | Yes | Yes | Partial — **this moved on 2026-09-12, but less far than it looks.** The repository still ships generation off: `.env.example` and `compose.yaml` carry `AIDA_MODEL_GENERATION_ENABLED=false` and default `AIDA_MODEL_ROUTE` to `openai-bank-sql`, so a fresh clone generates nothing. What exists now is local, gitignored operator configuration: two routes APPROVED in `sample-bank` through the real maker-checker path (`gemini-bank-sql` → `gemini-2.0-flash`, `openai-bank-sql` → `gpt-4o-mini`), credentials resolving, and no kill switch engaged. `compose.yaml` does pass `AIDA_MODEL_*` and both API keys through from the host, so the local topology can be pointed at a route | **No — and now for a concrete, live-proven reason rather than an absence.** Both approved routes were called end to end through `ProviderNeutralModelGateway.structured_completion` on 2026-09-12 and **both failed at the provider**: `gemini-bank-sql` returned HTTP 404 — *"This model models/gemini-2.0-flash is no longer available"*, i.e. the maker-checker-approved model identifier has been retired upstream — and `openai-bank-sql` returned HTTP 429 `billing_not_active`. A 404 is not in the gateway's retryable set `{408, 409, 429, 500, 502, 503, 504}` and the orchestrator only falls back on `{429, 502, 503, 504}`, so with `gemini-bank-sql` primary the fallback is never tried and every generated-SQL Ask fails closed. Generated-answer quality stays unmeasured because no approved route can currently answer | 2026-09-12 | AI governance | `src/aida/model_gateway.py`; `src/aida/agent_orchestrator.py`; `scripts/seed_model_route.py` (R11-B2, which registered the route through the real maker-checker path); `compose.yaml` `AIDA_MODEL_GENERATION_ENABLED: ${...:-false}`; README "Model generation is also fail closed" |
| Hybrid retrieval (lexical + vector + graph fusion) | Partial — and the limit is now measured rather than assumed: the vector channel **re-ranks the policy-authorized candidate set and cannot discover**, so it can reorder what lexical retrieval found but never surface a table it missed | Yes | Partial — **the 2026-09-06 "Yes" was wrong.** The vector third of the fusion is still not configured by anything this repository *ships*: `embedding_provider` defaults to `unset`, which `resolve_embedding_provider` turns into a refusal, and `compose.yaml` defines **no `AIDA_EMBEDDING_*` variable at all**, so no host `.env` can reach the containers. Confirmed live on 2026-09-12: `AIDA_EMBEDDING_PROVIDER` is empty inside the running `api` container, so `run_vector_channel` skips on `EmbeddingUnavailable` and the fusion there is lexical + graph. A process run directly against this machine's gitignored `.env` does reach a provider — which is how the Verified evidence was produced, and is not the same thing as the shipped topology being configured | Partial — the vector signal has now been exercised for the first time. R11-B2's 2026-09-12 benchmark run reports it "available and exercised" against live `gemini-embedding-001`, and the provider was separately confirmed to answer (one call, 768-dimension non-zero vector). **The measured result is the point:** hit@1 `0.5882`, recall-within-bound `0.7059` and MRR `0.6373` — unchanged from the vector-absent baseline — with all five semantic-paraphrase cases still not found, for the re-rank-not-discover reason above. No large-catalog benchmark; and CI `quality-baseline` supplies no embedding credential, so the CI gate still measures the vector-absent path | 2026-09-12 | Retrieval | `src/aida/retrieval.py`; `src/aida/retrieval_stages.py`; `src/aida/embedding_provider.py`; CI `quality-baseline` job; `Docs/90-reference/quality-benchmark-results.md`; `Docs/10-architecture/19-embeddings-design.md` §2 |
| Ask Atlas returns result rows to the user | Yes — bounded result panel with masking chips, column tags and a truncation banner; no result retention added | Yes | Yes | Partial — **both halves of the 2026-09-06 cell are now false.** It said "not validated in a real browser": step 5 of the CI `ui-journey` job drives Ask in real Chromium through the production nginx image and asserts the answer panel renders. It also said "the response carries no `applied_row_limit`": the response has carried `applied_row_limit` *and* `row_limit_source` since 2026-09-06, and `QueryResultTable.tsx` decides truncation from them via `truncatedByGatewayCap` instead of pattern-matching the SQL — that clause was wrong on the day it was written. What is genuinely unverified is the destination: the journey's upstream is a stub synthesised from the OpenAPI baseline, so no **real** query result has been rendered in a browser | 2026-09-12 | Frontend | F20; `ui-next/src/components/QueryResultTable.tsx`; `src/aida/query_execution_view.py`; `e2e/tests/journey.spec.ts`; CI `ui-journey` job |
| Steward agent — contracted proposals of table and column descriptions and glossary links | Yes — the deterministic GL-9, GL-8 and column-drafter producers run under an `AgentContract`; tier-gated, killable mid-run, bounded, ledgered; decides nothing. Where it is registered, the ingest side-car drafts under the same contract | Yes — `GET`/`POST /v1/organizations/{org}/steward-agent[/run]` mounted in `aida.main`; a console screen in the Steward work area. Runs are started by a person, or by the scheduler once its interval setting is above 0 (it is 0, off, by default) | **No** — nothing ships registered: an organization must approve an `AGENT`-kind AI asset version and give it a contract for `agent:steward` | **No** — in-memory SQLite and a fixture-mode browser check only; the mid-run kill rollback is not reproduced on PostgreSQL, and no run against a real estate | 2026-09-10 | Governance | ADR-0029; `src/aida/steward_agent.py`, `tests/test_steward_agent.py` |
| Lineage agent — contracted parsing of the view definitions and routine bodies ingestion captured | Yes — `sql_lineage_parser` over eligible definitions (active, available, literal-redacted, screened clean) of views with no parsed lineage, and `procedure_lineage` over eligible routine bodies with no routine-aware lineage, proposing only their table-to-table edges; edges written PROPOSED whatever the auto-activation settings say; tier-gated, killable mid-run, bounded by its pending edges, ledgered; decides nothing | Yes — `GET`/`POST /v1/organizations/{org}/lineage-agent[/run]` mounted in `aida.main`; a console screen in the Steward work area; its edges are decided in the parsed-lineage review queue (ADR-0026). Runs are started by a person, or by the scheduler once its interval setting is above 0 (it is 0, off, by default) | **No** — nothing ships registered: an organization must give an approved `AGENT`-kind AI asset version a contract for `agent:lineage` | **No** — in-memory SQLite, and the routine table's review migration applied to PostgreSQL by the ORM-drift gate; no run against a real estate | 2026-09-11 | Governance | ADR-0029 amendments; `src/aida/lineage_agent.py`, `src/aida/routine_lineage_edges.py`, `tests/test_lineage_agent.py` |
| Quality agent — contracted proposals of DQ-4 threshold rules | Yes — row-count floors and null-rate ceilings derived from each table's recent completed profiles, value-free; every proposal is a T2 `QUALITY_RULE_PROPOSAL` review a person decides, and approval creates the rule; a rule key already covered by a rule or a proposal is never proposed again | Yes — `GET`/`POST /v1/organizations/{org}/quality-agent[/run]` mounted in `aida.main`; a console screen in the Steward work area. Runs are started by a person, or by the scheduler once its interval setting is above 0 (it is 0, off, by default) | **No** — nothing ships registered: as above, for `agent:quality` | **No** — in-memory SQLite, and its migration applied to PostgreSQL by the ORM-drift gate; no run against a real estate | 2026-09-11 | Data quality | ADR-0029 amendment; `src/aida/quality_agent.py`, `src/aida/quality_rule_proposals.py`, `tests/test_quality_agent.py` |
### Certification not held

These are listed so their absence is a recorded state rather than an omission. All four columns are
**No** and the owner is a decision the bank has not yet supplied.

| Capability | Date | Evidence |
|---|---|---|
| Bank-scale load, soak, spike and chaos testing | 2026-09-12 | REVIEW §5: "No bank-scale load, soak, recovery, or connector certification was run". Re-checked 2026-09-12: CI's `perf-baseline` is a regression gate on a committed baseline, not a load, soak or chaos run |
| Penetration test / SAST / DAST | 2026-09-12 | REVIEW §6: "This is a technical review, not a claim of regulatory or penetration-test certification". Re-checked 2026-09-12: CI holds `dependency-scan`, `frontend-dependency-scan` and `secret-scan` — dependency and secret scanning, which are not SAST, DAST or a penetration test; no CodeQL, Semgrep or ZAP job exists |
| Backup, restore and regional failover | 2026-09-12 | REVIEW §5 "Recovery"; POINTS-TRACKER T28. Re-checked 2026-09-12: no backup, restore or failover script or runbook exists in the tree |
| Interactive WCAG AA accessibility conformance | 2026-09-12 | Still not held, and now precisely scoped rather than merely asserted. [`24-accessibility-acceptance-2026-09-12.md`](24-accessibility-acceptance-2026-09-12.md) splits the row: its §1 automated half **is** done and gated in CI (axe-core WCAG 2.1 A/AA across all 44 navigable screens, a real Tab driver, focus-containment and accessible-name floors); its §2 human half — screen reader, real-rendering colour contrast, 100% zoom and multiple screens — needs a person and has not been executed. `color-contrast` is deliberately not run in the automated harness because jsdom lays nothing out. F21: "ARIA alone does not establish working interaction" |
| Fresh-browser authentication through the real topology | 2026-09-12 | F06; POINTS-TRACKER §7 marks this ⚠ "Needs a deployed topology". Re-checked 2026-09-12: CI `ui-journey` does drive a real browser through the production nginx image, but the seat is a proxy-injected identity in front of a stub upstream, and `compose.oidc.yaml`'s mock-IdP overlay was not brought up — so no fresh browser has authenticated through a real issuer against the real backend |

## Keeping this register true

1. **A row changes when the code changes, in the same change.** If a pull request makes something
   reachable, it moves the Reachable cell in that pull request.
2. **Verified never moves without new evidence a reader can open.** A tracker row saying DONE is
   not evidence. A CI job name, a recorded run, or a retrieved object is.
3. **Removing a row requires removing the capability.** A contested or embarrassing row stays.
4. **The accomplishment log is not this document.** `06-accomplishment-log.md` is append-only
   history and records what was true on the day it was written. This register records what is true
   now, and is edited in place.

## Appendix: what the 2026-09-12 re-measurement changed

Recorded here because the value of a register is in what moved and why, and because two cells
turned out to have been **wrong on the day they were written** rather than merely out of date.
That distinction is the one worth keeping.

**Wrong when measured, now corrected.**

1. *Maker-checker review (bulk decision)* said "a concurrent PostgreSQL reproduction is still
   outstanding". `tests/test_governance_decision_postgres_concurrency.py` was added on
   2026-09-06 — the same day the row was measured — and is exactly that reproduction. The row
   understated the project for six days.
2. *Ask Atlas returns result rows* said "the response carries no `applied_row_limit`". It has
   carried `applied_row_limit` and `row_limit_source` since 2026-09-06, and the UI already
   decided truncation from them.
3. *Hybrid retrieval* said `Configured: Yes`. The vector third of the fusion has never been
   configured by anything this repository ships, and `compose.yaml` still defines no
   `AIDA_EMBEDDING_*` variable, so the cell claimed a capability the shipped topology could not
   exercise. Corrected to Partial.

**Stale, not wrong** (true when written, overtaken since): the browser OIDC row's "no IdP is
configured in this repository" — `compose.oidc.yaml` landed 2026-09-10; the `ui-next` row's "no
interactive or accessibility certification"; and the MCP row's missing JSON-RPC `initialize`.

**Verified moved on evidence produced in this pass** — three rows, each with the run recorded:

* *MCP endpoint* → Yes. `POST http://localhost:3001/mcp` through the running `ui-next` nginx with
  an `X-Principal-Id` header returned HTTP 200 and a JSON-RPC `initialize` result
  (`protocolVersion 2025-03-26`).
* *Maker-checker, single and bulk* → Yes. Run against the real PostgreSQL in the local stack,
  into a scratch database so no shared data was touched:

  ```
  AIDA_DECISION_CONCURRENCY_TEST_DATABASE_URL=postgresql+asyncpg://…/aida_r11b14_decision_race \
    python -m pytest tests/test_governance_decision_postgres_concurrency.py -p no:randomly
  # 14 passed in 10.07s
  ```

**A new defect this pass found, which belongs in tracker section P rather than here.** The
`gemini-bank-sql` route is APPROVED through maker-checker but its approved model identifier,
`gemini-2.0-flash`, has been retired by the provider and answers HTTP 404. Because 404 is not in
the orchestrator's fallback set, a deployment with that route primary fails every generated-SQL
Ask closed and never tries its approved fallback. Approving a route does not keep the upstream
model alive, and nothing in the platform currently notices when one disappears. R11-B2's
benchmark deliberately places no provider call ("kept out of a routine benchmark run to avoid an
unbounded-cost, unbounded-network side effect"), so the two calls made in this pass appear to be
the first live generation attempts against either approved route — which is why a retired model
had gone unnoticed.

**What could not be measured, and what it would take.** Disaster recovery, backup/restore and
regional failover need a deployed topology and an approved RPO/RTO. Oracle, BigQuery, Snowflake
and Databricks need real accounts. The SOC and notification destinations need a real collector and
a real channel. The WORM archive against real AWS S3 needs an AWS bucket, where refusals are
`403 AccessDenied` and IAM is in play rather than Object Lock alone. The human half of
accessibility needs a person with assistive technology. Generated-answer quality needs an approved
route whose model the provider still serves. Each of these is a legitimate **No**, not a gap in
this pass.

## Related documents

* [`00-status.md`](00-status.md) — the narrative status page; this register is its current-state companion
* [`03-tracker.md`](03-tracker.md) — item-level open work, one row per ID
* [`../review-2026-09-05/REVIEW.md`](../review-2026-09-05/REVIEW.md) — the findings referenced above
* [`../review-2026-09-05/POINTS-TRACKER.md`](../review-2026-09-05/POINTS-TRACKER.md) — remediation status for those findings


## September 17 - Ask layout and history request isolation

Source verification for R11-UX16, not deployment certification. Ask keeps a compact collapsible history and a full-width question row; selector guidance wraps on narrow layouts. First-page and paginated requests are cancelled and invalidated on datasource changes, refresh or unmount, so late source-A responses cannot populate source-B history. Pagination cannot start while hidden and stops on empty pages or errors instead of looping. Existing answer/product provenance behavior is preserved.

Evidence: `ui-next/src/screens/AskScreen.tsx`, `AskScreen.css` and `AskScreen.test.tsx`; Ask plus the automated accessibility sweep passed 72 tests, and the TypeScript/Vite production build passed on 2026-09-17. This does not verify deployed browser geometry, screen-reader reading order or the planned Results/Query/Evidence views. Current status and remaining acceptance live in [tracker section P](03-tracker.md#p-current-execution-queue-reconciled-2026-09-11), R11-UX16. GraphQL and OKF remain pickup work, not verified capabilities.
