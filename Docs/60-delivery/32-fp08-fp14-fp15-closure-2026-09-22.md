
# Closing FP08, FP14 and FP15: nothing left to build, 2026-09-22

A dated addendum to the [2026-09-22 addendum](31-last-open-items-2026-09-22.md): evidence, not a
queue (`CLAUDE.md`, rule 2). Status lives in
[tracker section P](03-tracker.md#p-current-execution-queue-reconciled-2026-09-11).

**Asked for:** the same "build if needed, otherwise drop" pass on the PARTIAL rows named as having
nothing left to build, starting with the three where nothing was proposed to build at all
(R11-FP08, R11-FP14, R11-FP15).

**Checked before closing, not taken on report:**

- **R11-FP08.** Its own last dated note said "Packages still wait on FP03." That is stale.
  `tests/test_routine_description_body_states.py::test_a_package_is_refused_by_name_and_not_silently_skipped`
  shows the actual, permanent shape: a package MEMBER is a plain `MetadataRoutine` row with
  `package_name` set — exactly what R11-FP03's later slices gave it — so it already gets a
  description through the same path as any other routine (`is_describable_routine() is True`). The
  PACKAGE container itself is refused with `PACKAGE_NOT_DESCRIBABLE`, permanently and by design: "a
  package is a container for subprograms, not a callable unit with a signature, a return type or
  lineage of its own, so a description of one would be a description of nothing." There is no
  package-description feature left to build; the row's own note just never caught up to what FP03
  had already delivered.
- **R11-FP14.** Its own "Remaining" bullet names an operator action (a customer organization must
  approve an AGENT-kind version and contract before a proposed tool can run — not this repository's
  work) and three limits the row itself states are deliberate: a hand-written copy of a query with
  different aliases or clause order is missed on purpose; an undocumented or unnamed built-in
  function is refused until an operator names it (the adversarial denylist is the point, not a gap);
  and native invocation is R11-FP18, a separate row already deferred. None of that is planned code.
- **R11-FP15.** Its own "Remaining" bullet says acting on permission-change signals "was decided
  against" by R11-FP16. Read that row directly: 2026-09-15, "Meaning signals acted on, permission
  signals decided" — because a source-side grant authorizes nothing inside Atlas, there is nothing
  for a permission-change signal to drive. A decision not to build something is not open work.

**Closed 2026-09-22, this session:** all three PARTIAL → DONE, with the note above appended to each
row (`03-tracker.md`). No source code changed; this is a documentation correction, not a build.

## The counts, after this

95 DONE, 24 PARTIAL, 7 BLOCKED, 0 TODO, 22 DEFERRED, 2 CANCELLED over 150 rows; 31 of the 126 active
rows are open, and every one is PARTIAL or BLOCKED — unchanged from the day before in kind, three
fewer in number.

**What is left among the PARTIAL rows**, grouped the way the previous session already grouped it
(unchanged by this pass): the customer's environment (7 BLOCKED); a real outside system (B9, B10, I1,
C14, FP02, D17); a person's decision (C2, FP04, FP06, FP05, FP13, SQL01, OKF03); real-world tuning
(GQL01, FP16, FP17); and real code still to write (FP01, FP03, FP07, FP09, FP12, OKF02, REV01, S13) —
each of those eight is a multi-day engineering item and is not touched by this pass.
