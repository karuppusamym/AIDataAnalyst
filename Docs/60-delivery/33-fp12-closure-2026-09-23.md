# Closing FP12: what moved under a context product, shown with no click, 2026-09-23

A dated addendum ([CLAUDE.md](../../CLAUDE.md), rule 2): evidence, not a queue. Status lives in
[tracker section P](03-tracker.md#p-current-execution-queue-reconciled-2026-09-11).

**Asked for:** take the PARTIAL rows with code left, one at a time. R11-FP12 first. The other six
(FP01, FP03, FP07, FP09, OKF02, REV01) were taken by a peer session, by agreement; this addendum
covers FP12 only.

**FP12's remainder, as the row stated it:** a stale badge that needs no click (the list row carried
no reading; a passive badge needed a server-side field); the same check on the agent gateway's
exposure list, Ask's product picker and the rollout version list; and a pinned meaning version that
has moved on, a separate signal no screen showed. A table reshape stays deliberately uncounted.

## What was built

| Commit | Change |
|---|---|
| `8ce9ae7` | `load_changes_since_published_counts`: the per-version reading (`load_coverage_changes`) for many versions in four queries, attributed per version in Python against its own ids and publication time. `GET /v1/projects/{project_id}/context-products/changes-since-published` serves it, one row per product's latest version, or with `product_id` every version of one product. Its own route rather than a field on the product list, because that list admits Viewer, Auditor, Reviewer and SemanticAdmin and every coverage door refuses them: the route admits `COMPILER_ROLES` only. `null` means never published (no baseline), which is not 0. |
| `042c3a0` | The badge with no click on the Context products rows (the on-demand check, which says which subjects moved, replaces it once run), the agent gateway's exposure rows, Ask's picker options and a note under it, and the rollout version select and pinned rows. One read per screen, under `readDecision` on the coverage roles. |
| `3ad8bba` | `meaning_moved` on the same route (`load_pinned_meaning_moved_counts`, three queries, `load_pinned_meaning`'s own rule for "current"), shown as a separate "pinned meaning moved on" badge that stays after the on-demand check. |

## Checks

| Check | Result |
|---|---|
| `tests/test_r11_fp12_changes_summary.py` | 13 passed: the batched count equals the per-version reading for two versions with different baselines and scopes (moves before, between and after, text replaced and restored, an identical re-approval, an out-of-scope routine, another organization's signal); the same query count for 1 and 22 versions (at most 4); the route's visibility, truncation, refusal of another organization's project, the rollout's product mode and 404; the matrix row names exactly `COMPILER_ROLES`; the meaning count equals `load_pinned_meaning`'s before and after a model is superseded and a term deprecated |
| Every context-product test file | 1,386 passed (before the meaning part); the FP12 suites and route gates after it, 103 passed |
| Static gates | all 18 pass after the shim register and architecture map were regenerated; full mypy clean on 430 files |
| UI | `tsc` clean; the four screens' files and the client 120 tests, with two mutants caught (the role gate removed; the rollout's product scope dropped). Under another session's Docker build at 100% CPU a few timing tests (AskScreen history, a11y-sweep `relationships`, App routing) fail, and fail identically on the unchanged parent commit; each passes alone |
| Playwright journeys (isolated stub stack) | 46 passed |
| Stack rebuilt from a clean worktree | parity 9 of 9; the route answers DataSteward 200 and Viewer 403; Northwind's published product reads 0 changes and its product in review `null` |
