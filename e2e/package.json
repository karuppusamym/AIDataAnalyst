{
  "name": "atlas-e2e",
  "private": true,
  "version": "0.1.0",
  "description": "Browser journey suite (R11-B11). Deliberately a separate package from ui-next: it is test infrastructure, it must never be installed into the shipped ui-next image (whose Dockerfile runs `npm ci` before building the SPA), and keeping it out of ui-next/package-lock.json avoids a known multi-session collision file.",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0"
  }
}
