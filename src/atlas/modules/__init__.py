"""The 21 bounded-context modules (`Docs/10-architecture/04-module-decomposition.md`
Sec.4, the module register).

Each `atlas.modules.<name>` package follows the same anatomy and is created
with `scripts/generate_module.py`. This directory holds only module
packages -- no code of its own.
"""
