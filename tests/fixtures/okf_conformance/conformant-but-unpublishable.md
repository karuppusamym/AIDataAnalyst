---
type: Metric
title: Conformant OKF that Atlas still refuses to publish
---

An external link to [an upstream page](https://example.com/docs), which OKF permits and Atlas
does not export by default. And a fenced block, which would only ever be needed for definition
text:

```sql
SELECT 1
```
